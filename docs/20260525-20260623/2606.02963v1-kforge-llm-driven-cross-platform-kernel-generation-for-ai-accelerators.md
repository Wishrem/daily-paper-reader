---
title: "KForge: LLM-Driven Cross-Platform Kernel Generation for AI Accelerators"
title_zh: KForge：基于LLM的跨平台AI加速器内核生成
authors: "Taras Sereda, Burak Bartan, Ankita Nayak, Tom St. John, Natalie Serrino, Zain Asgar"
date: 2026-06-01
pdf: "https://arxiv.org/pdf/2606.02963v1"
tags: ["query:ai-infra"]
score: 9.0
evidence: 利用LLM为异构AI加速器自动生成跨平台内核，实现异构混合推理
tldr: "针对AI加速器异构化导致的手工编写内核效率低下的问题，提出KForge框架。该框架采用两个LLM代理协作迭代：生成代理负责生成并修正内核代码，性能分析代理解读剖析数据并给出优化建议。在NVIDIA B200上，端到端吞吐量比TensorRT-LLM提升2.12%；在Intel Arc B580上，Triton内核相比PyTorch baseline获得5.13倍几何平均加速。KForge实现了跨平台内核的自动生成与优化。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.02963v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1651, \"height\": 565, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.02963v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1231, \"height\": 387, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.02963v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 788, \"height\": 173, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.02963v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1394, \"height\": 518, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.02963v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 907, \"height\": 248, \"label\": \"Table\"}]"
motivation: 异构AI加速器增多，手工编写跨平台高性能内核耗时且难以扩展，需要自动化方案。
method: 构建迭代精炼循环，由生成代理和性能分析代理协作，交替进行功能性修正和性能优化。
result: "NVIDIA B200上gpt-oss-20b推理吞吐提升2.12%；Intel Arc B580上GEM+尾算任务加速5.13倍。"
conclusion: KForge有效实现跨平台内核自动生成，性能接近甚至超越手工优化基线。
---

## 摘要
生产推理日益面向异构加速器混合部署。智能体流水线交织了推理、工具调用和多智能体协调，每个阶段具有不同的计算和内存特征。为达到最优效率，每个阶段应在最适合它的加速器上运行。这带来了系统挑战：每条流水线现在需要跨日益增多的硬件后端和编程模型的高性能内核。手工编写这些内核耗时、需要深厚的底层专业知识，且随着内核复杂度增长难以扩展。最近，大语言模型（LLM）被用于自动内核生成，但底层代码生成和跨后端泛化方面的挑战依然存在。我们提出KForge，一个跨平台框架，其核心是由两个协作的LLM智能体驱动的迭代优化循环：一个生成智能体，利用编译和正确性反馈生成并逐步优化内核；一个性能分析智能体，解释从程序化API到基于GUI的工具的性能分析数据，并发出指导下一轮合成的建议。该循环在功能传递（推动候选内核达到正确性）和优化传递（缩小与手工调优基线的性能差距）之间交替。我们在两个基线参考可用性差异很大的后端上评估KForge。在NVIDIA B200上，KForge在gpt-oss-20b推理速度基准测试中相比TensorRT-LLM实现了2.12%的端到端吞吐量提升。在Intel Arc B580上，KForge生成的Triton内核在KernelBench Level 2的37个GEMM+尾部操作工作负载上，相比PyTorch即时执行和torch.compile中较快的那个，实现了5.13倍的几何平均加速，主要通过算子融合和混合精度执行实现。

## Abstract
Production inference increasingly targets a heterogeneous mix of accelerators. Agentic pipelines interleave reasoning, tool calls, and multi-agent coordination, each with distinct compute and memory profiles. For optimal efficiency, each stage should run on the accelerator best suited to it. This creates a systems challenge: each pipeline now requires high-performance kernels across a growing set of hardware backends and programming models. Writing these kernels by hand is time-consuming, demands deep low-level expertise, and does not scale as kernel complexity grows. Recently, Large Language Models (LLMs) have been leveraged for automatic kernel generation, but challenges in low-level code generation and cross-backend generalization persist. We present KForge, a cross-platform framework built around an iterative refinement loop driven by two collaborating LLM-based agents: a generation agent that produces and progressively refines kernels using compilation and correctness feedback, and a performance-analysis agent that interprets profiling data, from programmatic APIs to GUI-based tools, and emits recommendations that steer the next round of synthesis. The loop alternates between functional passes, which drive a candidate to correctness, and optimization passes, which close the performance gap to hand-tuned baselines. We evaluate KForge on two backends with very different baseline reference availability. On NVIDIA B200, KForge achieves a 2.12$\%$ improvement in end-to-end throughput compared to TensorRT-LLM on the gpt-oss-20b inference speed benchmark. On Intel Arc B580, KForge generates Triton kernels achieving a 5.13$\times$ geometric mean speedup over the faster of PyTorch eager and torch.compile on 37 GEMM + tail-ops workloads from KernelBench Level 2, primarily via operator fusion and mixed-precision execution.

---

## 论文详细总结（自动生成）

# KForge: LLM-Driven Cross-Platform Kernel Generation for AI Accelerators 中文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：生产推理部署正转向异构加速器混合环境（NVIDIA、AMD、Intel、Apple），不同推理阶段需不同硬件。但为每个后端手工编写高性能内核耗时、需底层专业知识且难以扩展。大语言模型（LLM）被用于自动内核生成，但面临低层代码生成脆弱性和跨后端泛化不足的挑战。
- **整体含义**：本文旨在构建一个通用、自动化的跨平台内核生成与优化框架，使得LLM能像人类内核工程师一样，通过迭代反馈生成正确且高性能的代码，从而加速异构推理系统的开发和部署。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：采用两个LLM智能体协作的迭代精炼循环（Iterative Refinement Loop），交替进行功能修正和性能优化。
- **关键技术细节**：
  - **生成代理 (Generation Agent)**：接收任务提示（包括PyTorch参考实现、目标编程模型等），生成候选内核代码（含内核程序、调度代码、JIT编译代码、PyTorch模型类）。通过编译错误、运行时错误、数值/形状不匹配等反馈进行功能修正，直至正确。
  - **性能分析代理 (Performance-Analysis Agent)**：处理性能剖析数据（文本或截图，如Nsight Systems、Xcode Instruments），识别瓶颈（如内存带宽利用率、warp占用等），输出单一代码级优化建议，供下一轮生成使用。
  - **流程**：初轮由生成代理产生候选 → 编译执行 → 若失败则追加错误反馈 → 直至正确 → 切换至优化阶段：收集性能数据 → 性能分析代理提出优化建议 → 生成代理据此改进 → 重复直至性能收敛。
  - **跨平台支持**：通过统一提示接口支持4家厂商（NVIDIA, AMD, Intel, Apple）和6种编程模型（CUDA、Triton、CuTe DSL、HIP、SYCL、Metal）。
  - **其他特性**：候选护栏（禁止/要求代码模式）、提示编辑、辅助代码注入、结构化工件保存、自定义测量钩子。

## 3. 实验设计：数据集、场景与基准

- **场景一：NVIDIA B200 端到端推理优化**
  - **任务**：优化gpt-oss-20b（MoE架构）的解码路径内核。
  - **基准**：TensorRT-LLM v1.3.0rc9（手工调优多年）。
  - **选择三个有源码的内核**：Fused Add+RMSNorm、MoE finalize、Bias+RoPE+KV update。
  - **评估**：微基准测试（各batch size下的执行时间）和端到端吞吐量（512请求，1024预填充，8192解码，batch size 8，7次独立迭代）。
  - **对比方法**：基线（TensorRT-LLM） vs. KForge优化后集成版本。

- **场景二：Intel Arc B580 Triton内核生成**
  - **任务**：为Intel Arc B580显卡生成Triton内核。
  - **基准**：KernelBench Level 2中的37个GEMM+尾部操作问题。
  - **对比方法**：torch.compile 与 PyTorch eager模式中较快的那个（作为基线）。
  - **评估**：每个问题运行5次生成-精炼循环，取最好内核，对比基线执行时间。

- **具体实验设置**：LLM使用Claude Opus 4.6（高努力模式）。NVIDIA实验固定GPU时钟1500 MHz，禁用自动调谐器以确保可重复性。

## 4. 资源与算力

- **文中明确说明**：KForge框架由Claude Opus 4.6驱动（高努力模式）。目标硬件为NVIDIA B200和Intel Arc B580。
- **未明确说明**：LLM推理所使用的具体GPU型号、数量、训练时长等算力信息；也未提及KForge自身的运行成本或超参数调优开销。因此无法评估其计算资源投入。只可推断实验在本地或云端完成，但细节缺失。

## 5. 实验数量与充分性

- **实验数量**：
  - NVIDIA端：3个内核的微基准测试（7个batch size），以及端到端7次独立迭代。
  - Intel端：37个KernelBench Level 2问题，各5次迭代，共37个独立场景。
- **充分性分析**：
  - **正面**：NVIDIA实验控制了autotuner可变性，7次迭代变异系数<0.1%，结果可靠。Intel实验覆盖了常见尾部操作模式（激活、归一化、LogSumExp等），几何平均加速具有代表性。
  - **不足**：仅覆盖两个后端，未在AMD、Apple上做定量实验。只使用了单一LLM模型（Claude Opus 4.6），缺乏不同模型间的对比。未进行全面的消融研究（如去掉性能分析代理或交叉翻译的效果）。实验场景较窄（仅GEMM+尾部操作和一个模型推理），泛化证据有限。
- **公平性**：NVIDIA端与TensorRT-LLM公平对比（同样配置固定频率）；Intel端对比标准PyTorch编译方案，且未使用专有手工库，基准合理。但未与其它LLM内核生成系统（如KernelBlaster、CUDA-LLM）直接对比。

## 6. 论文的主要结论与发现

- **NVIDIA B200端到端**：集成KForge优化内核后，系统吞吐量提升2.12%（从2601.55 token/s到2656.61 token/s），总执行时间减少2.07%。在已高度优化的TensorRT-LLM基础上实现正向增益，且小幅度提升在规模化部署中可复合放大。
- **Intel Arc B580**：KForge生成的Triton内核在37个问题上的几何平均加速为5.13×，典型技术包括：层融合、混合精度（FP16 matmul + FP32累加）、单通归约、自适应分区。
- **结论**：LLM驱动的代理式内核合成已成为生产级优化和快速新兴硬件起量的实用工具，能够跨越不同硬件生态自动生成竞争性或优越的内核。

## 7. 优点

- **双代理分工设计**：分离生成和性能分析角色，避免长上下文信息丢失，并允许为不同任务分配不同LLM模型（模块化）。
- **广泛的跨平台支持**：覆盖4家厂商、6种编程模型，统一接口，通过交叉翻译利用已有的CUDA实现生成其他平台代码。
- **功能-优化交替循环**：先确保正确性再优化，符合工程实践，减少无效迭代。
- **丰富的工程特性**：栅栏、提示编辑、辅助代码注入、自定义测量钩子，便于集成到实际工作流。
- **严谨的实验验证**：在NVIDIA端控制了变量（固定频率、禁用autotuner），结果可重复；在Intel端提供了多问题统计，加速比稳定。
- **关注端到端性能**：不仅评估孤立内核，还在NVIDIA上测量了全模型推理吞吐，更具实际意义。

## 8. 不足与局限

- **源框架依赖**：目前仅支持PyTorch作为源框架，未验证JAX等。
- **核心用例依赖参考源码**：案例均基于有源码的生成本，对于只提供封闭二进制（如cubin）的算子（如融合注意力），系统无法从行为规范直接合成，限制了实际应用范围。
- **实验覆盖有限**：仅测试了两个后端，且Intel案例没有与专有手工库（如Intel oneAPI）对比；未在AMD、Apple上展示定量结果。
- **损失函数单一**：静态性能剖析可能无法捕捉内核间交互（如寄存器压力对相邻核的影响），孤立优化未必总是提升端到端，文中承认该风险。
- **LLM选择单一**：仅用Claude Opus 4.6，未与其他LLM（如GPT-4、开源模型）对比，也未讨论不同LLM对性能的影响。
- **未说明评估鲁棒性**：引用了Sakana AI的CUDA Engineer漏洞案例，但未描述KForge是否采用了额外验证来防止类似评估欺骗。
- **缺少计算资源报告**：未提供LLM推理的硬件配置和能耗信息，可重复性较差。

（完）
