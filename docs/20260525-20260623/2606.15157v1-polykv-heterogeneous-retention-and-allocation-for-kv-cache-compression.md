---
title: "PolyKV: Heterogeneous Retention and Allocation for KV Cache Compression"
title_zh: PolyKV：KV缓存压缩的异构保留与分配
authors: "Chao Fei, Panos Kalnis"
date: 2026-06-13
pdf: "https://arxiv.org/pdf/2606.15157v1"
tags: ["query:ai-infra"]
score: 9.0
evidence: 层间异构KV缓存压缩框架
tldr: "现有KV缓存压缩方法通常对所有层采用统一压缩策略和均匀缓存预算，忽略了不同层在预填充和解码阶段的差异。PolyKV提出层级别KV缓存优化框架，通过基于层信号的策略选择和预算分配，实现异构组合现有方法。在LLaMA-3.1-8B和Qwen3-8B上，512平均KV预算下恢复与FullKV性能差距的54.5%和25.7%，预算扫描中持续改进最强基线1.7%-6.4%。该工作证明了层间异构分配的有效性，为长上下文推理提供更高效方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.15157v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1406, \"height\": 552, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15157v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1372, \"height\": 707, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15157v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1422, \"height\": 519, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.15157v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1454, \"height\": 1064, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15157v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1260, \"height\": 243, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15157v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 662, \"height\": 303, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15157v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 782, \"height\": 451, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15157v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 578, \"height\": 454, \"label\": \"Table\"}]"
motivation: 现有KV缓存压缩采用统一策略和均匀预算，忽视不同层的角色差异，限制了长上下文推理的内存效率。
method: PolyKV为每层选择合适压缩策略并分配非均匀缓存预算，实现层间异构优化，组合现有方法。
result: "在LLaMA-3.1-8B和Qwen3-8B上，512预算下恢复FullKV差距的54.5%和25.7%，预算扫描中提升基线1.7%-6.4%。"
conclusion: 层级别异构KV缓存压缩能显著缩小与FullKV的性能差距，在固定预算下持续优于单一策略基线。
---

## 摘要
KV缓存压缩对于减少长上下文大语言模型推理的内存成本至关重要。然而，现有方法通常对所有Transformer层采用单一的压缩策略和统一的缓存预算。这种统一设计忽略了不同层在预填充和解码阶段可能扮演不同角色，因此可能需要不同的逐出策略和缓存容量。我们提出PolyKV，一种逐层KV缓存优化框架，该方法选择与预算分配共同考虑设计空间。PolyKV基于层级别信号为每一层路由到合适的KV压缩策略，同时在固定总预算下分配非均匀预算。这种公式能够实现现有KV缓存方法的异构组合。在LLaMA-3.1-8B和Qwen3-8B上的实验表明，在相同的512 token平均KV预算下，PolyKV分别恢复了最强单一策略基线与FullKV之间LongBench性能差距的54.5%和25.7%。在128-1024预算扫描范围内，PolyKV相比最强基线持续提升了1.7%-6.4%，对应FullKV差距的40.0%-54.5%恢复。

## Abstract
KV cache compression is essential for reducing the memory cost of long-context large language model inference. Existing approaches, however, typically apply a single compression policy and a uniform cache budget across all transformer layers. This uniform design ignores the fact that different layers can play different roles during prefill and decoding, and may therefore require different eviction strategies and cache capacities. We present PolyKV, a layer-wise KV cache optimization framework that considers design space with method selection and budget allocation. PolyKV routes each layer to a suitable KV compression policy based on layer-level signals, while assigning non-uniform budgets under a fixed total budget. This formulation enables heterogeneous compositions of existing KV cache methods. Experiments on LLaMA-3.1-8B and Qwen3-8B show that, under the same 512-token average KV budget, PolyKV recovers 54.5% and 25.7% of the LongBench performance gap between the strongest single-policy baseline and FullKV, respectively. Across 128-1024 budget sweep, PolyKV consistently improves over the strongest baseline by 1.7%-6.4%, corresponding to 40.0%-54.5% recovery of the FullKV gap.

---

## 论文详细总结（自动生成）

# PolyKV：KV 缓存压缩的异构保留与分配 —— 论文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：长上下文大语言模型（LLM）推理中，KV 缓存（Key-Value cache）随上下文变长急剧膨胀，成为内存瓶颈，限制批量大小和生成速度。现有 KV 缓存压缩方法通常对所有 Transformer 层采用**同一逐出策略（eviction policy）** 和**均匀的缓存预算**，忽略了不同层在预填充（prefill）和解码（decoding）阶段的角色差异。
- **核心问题**：文献已揭示 Transformer 各层在注意力模式、抽象表征、上下文功能和剪枝敏感度上存在异质性。因此，全局统一的压缩策略是**粗粒度的假设**，可能导致次优的存储-精度权衡。
- **整体意义**：PolyKV 提出将 KV 缓存压缩重新定义为**逐层（layer-wise）设计空间**，同时考虑“选择什么逐出模式”和“分配多少缓存预算”两个耦合维度，通过异构组合现有方法显著提升固定内存预算下的推理质量，为长上下文推理提供更高效、更鲁棒的缓存压缩方案。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：PolyKV 是一个**层级别、相位感知（phase-aware）的 KV 缓存优化框架**。它通过离线校准为每个 Transformer 层分别指定：① 在预填充和解码阶段应使用何种逐出策略（retention policy）；② 该层的缓存预算（容量）。在固定总缓存预算约束下，实现策略与预算的异构分配。
- **关键技术细节**：
    - **设计空间定义**：策略 Π = {({r_{l,φ}}, {b_{l,φ}})} 对每层 l 和每个相位 φ 指定逐出模式 r 和预算 b。总预算约束 ∑ b_{l,φ} = B_φ，b_{l,φ} ≥ b_min。
    - **离线校准**：通过小规模校准集 D_cal 收集两类层级别信号：
        - **逐出偏好信号**：衡量各候选模式在保持全 KV（FullKV）参考激活上的表现。使用**余弦相似度**（越大越好）和**PPL 敏感度**（越小越好）作为指标。校准只变动目标层的该相位，其余层保持 FullKV。
        - **缓存预算敏感度信号**：估计各层受缓存减少的影响程度。使用**注意力熵**（越高越需要大预算）和**PPL 敏感度**（各模式中的最大 PPL 退化）来表征。
    - **策略构建（Algorithm 1）**：
        1. 对每个层-相位对，根据偏好信号选择最优逐出模式（如余弦相似度最高或 PPL 退化最低）。
        2. 对每个相位，根据敏感度信号进行按比例预算分配：˜b_l = b_min + (B - L·b_min) · (S_l + ε) / Σ(S_j + ε)。再通过 Round_B 将连续预算取整为整数并确保总和为 B。
    - **运行时实现**：PolyKV 采用**钩子（hook）架构**，将不同逐出方法封装为策略（policy），每个策略实现一组钩子处理函数（如图 2 所示）。校准后的策略以 YAML 配置文件加载，通过轻量钩子注入推理流程，无需修改模型代码，易于扩展。

## 3. 实验设计

- **数据集/场景**：
    - **LongBench**：覆盖多任务长上下文理解的基准（21 个任务的宏观平均）。
    - **RULER**：用于受控压力测试的长上下文检索与追踪基准（包括 NIAH 检索、QA、单词抽取、变量跟踪等）。
- **Benchmark**：在 LongBench (21-task macro average) 和 RULER (global average) 上评估，指标为 **score**（越高越好）。
- **对比方法**：9 种代表性 KV 缓存压缩方法：H2O、TOVA、CAM、PyramidKV、SnapKV、D2O、CAKE、ScissorHands、StreamingLLM。所有方法使用**相同的总缓存预算**，公平比较。
- **模型**：LLaMA-3.1-8B 和 Qwen3-8B。

## 4. 资源与算力

- 论文中**未明确说明**具体的 GPU 型号、数量或训练（校准）时长。仅提及离线校准使用“small calibration set”（小校准集），但未给出具体计算资源消耗。因此无法量化算力投入。

## 5. 实验数量与充分性

- **主要实验数量**：
    - **主结果（表1）**：在 512 token 平均预算下，对两个模型在两个基准上报告 PolyKV 与最强基线及 FullKV 的对比。
    - **预算扫描（表2）**：在 LLaMA-3.1-8B 上，从 128 到 1024 token 平均预算共 5 个水平，比较 PolyKV 与最强基线。
    - **信号有效性消融（表3）**：针对逐出选择和预算分配信号，设计 forward（按信号正序）与 inverse（按信号逆序）对比，分为预填充、解码、两相多种组合。
    - **任务级分析（表4）**：对 LongBench 按类别（单文档 QA、多文档 QA、摘要、少样本、综合检索、代码）以及 RULER 子任务，报告 PolyKV 相对于最强基线的 delta 分数。
    - **案例研究（图3）**：展示了 LLaMA LongBench 512 预算下的代表性异构策略分布。
- **充分性评估**：
    - 实验覆盖了两种 8B 规模模型、两个互补基准（长理解 + 精确检索）、多种预算范围、多项消融（信号有效性、任务类别影响）、以及策略可视化。实验设计较为全面，对比方法覆盖主流压缩技术，公平性较好（固定总预算）。
    - 不足：仅测试了 8B 级别模型，未涉及更大规模（如 70B）或不同架构；校准集与评测集分离但未详细披露校准集大小及来源；RULER 上 Qwen3-8B 出现性能倒退，说明当前信号在精确检索任务上存在局限性。

## 6. 主要结论与发现

- **主结论**：在固定总 KV 缓存预算下，允许逐出模式和缓存容量按层异构分配能够**显著超越最强单一策略基线**。
    - LongBench（512 token 预算）：LLaMA-3.1-8B 上恢复与 FullKV 差距的 **54.5%**；Qwen3-8B 上恢复 **25.7%**。
    - 预算扫描（128-1024）：PolyKV 持续提升 1.7%–6.4%（相对最强基线），在高压缩（低预算）时增益最大。
- **增益来源**：**逐出模式选择**是主要贡献，尤其在预填充阶段。预算非均匀分配在预算较宽松时提供额外改进，但效果不如逐出选择稳健且更依赖模型。
- **任务依赖性**：在需要保留广泛上下文信息的 QA 和少样本任务上效果显著；在精确检索（RULER）和代码任务上表现较弱，说明校准信号对稀疏关键证据的保留不够可靠。
- **异构策略的可实现性**：PolyKV 提出的离线校准+静态方案可行，避免了在线搜索开销，且顶层配置（YAML）易于复现。

## 7. 优点

- **方法新颖性**：首次将 KV 缓存压缩形式化为**层级别双维度优化**（逐出模式 + 预算），打破了传统全局统一假设，提供了更精细的优化视角。
- **模块化实现**：基于钩子的架构高度可扩展，无需修改模型代码，易于集成新逐出方法，有利于社区后续研究。
- **消融全面**：通过 forward vs inverse 信号反转实验，清晰分离了逐出选择和预算分配各自的贡献，实验设计严谨。
- **跨模型和预算验证**：在两种不同家族（LLaMA、Qwen）的 8B 模型上验证，并在 128–1024 宽预算范围内测试，增强了结论的泛化性。
- **实用性**：离线校准一次、推理阶段静态使用，无需在线决策，易于部署。

## 8. 不足与局限

- **校准信号对精确检索任务不佳**：在 RULER 上 Qwen3-8B 性能下降（-3.98 分），说明当前基于余弦相似度和 PPL 的信号不能保证保留稀疏但关键的证据 token。需要探索更适应检索场景的校准指标。
- **模型规模有限**：仅测试 8B 模型，未验证在更大模型（如 70B）或不同架构（如 MoE）上的有效性。策略的泛化性未知。
- **预算分配信号较弱**：表 3 显示预算分配消融（熵和 PPL 敏感度）的 forward vs inverse 差异很小甚至为负（Qwen 上相反），表明当前预算敏感度信号不够可靠，还需改进。
- **校准集细节缺失**：未明确说明校准集的大小、来源（如是否来自 LongBench 训练集或其他分布），以及校准与评测集重叠控制，可能存在偏差风险。
- **计算开销**：离线校准需要为每个候选模式对每层执行一次 FullKV 与扰动运行，虽然规模可控但未定量报告校准时间/资源消耗。
- **任务覆盖**：LongBench 类别中代码任务出现负增益，RULER 上攻击性任务不及基线，说明 PolyKV 对某些类型的长上下文任务（如代码生成、精确检索）尚未优化充分。

（完）
