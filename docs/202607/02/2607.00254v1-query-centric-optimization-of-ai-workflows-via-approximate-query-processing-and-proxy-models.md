---
title: Query-Centric Optimization of AI Workflows via Approximate Query Processing and Proxy Models
title_zh: 基于查询的AI工作流优化：近似查询处理与代理模型
authors: "Huayi Wang, Jun Xu, Gromit Yeuk-Yin Chan"
date: 2026-06-30
pdf: "https://arxiv.org/pdf/2607.00254v1"
tags: ["query:ai-infra"]
score: 8.0
evidence: 使用近似查询处理和代理模型减少昂贵模型调用
tldr: "现代AI工作流可化为声明式查询，其昂贵谓词由大模型评估。本文提出两种查询中心优化策略：其一将工作流视为在线聚合问题，渐进采样并提前终止；其二训练轻量决策树预过滤记录。在TPC-DS上，AQP在10-15%调用下误差低于10%，PM减少60-70%调用。在LLM管道上，AQP在20-50%调用下准确率损失小于5%，PM加速高达19倍且损失小于10%。这些方法显著减少模型调用，无需修改底层模型或管道。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.00254v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 895, \"height\": 658, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00254v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 894, \"height\": 852, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00254v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 867, \"height\": 979, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00254v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1849, \"height\": 715, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00254v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1852, \"height\": 715, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00254v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1849, \"height\": 923, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00254v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1841, \"height\": 397, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00254v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1843, \"height\": 351, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00254v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1840, \"height\": 359, \"label\": \"Figure\"}]"
motivation: LLM后期训练及推理任务中，大模型或奖励函数调用昂贵，传统方法常需大量调用，效率低下。
method: 采用近似查询处理（AQP）在线聚合及代理模型（PM）决策树预过滤，仅对不确定记录调用昂贵模型。
result: "TPC-DS上AQP节省85-90%调用，PM节省60-70%；LLM管道上AQP节省50-80%调用，PM加速高达19倍。"
conclusion: 基于查询中心的优化有效减少AI工作流中模型调用，保持高精度，可推广至多种管道。
---

## 摘要
许多现代AI工作流，从大语言模型后训练管道到智能体推理任务，都可以表示为声明式查询，其代价高昂的谓词由大型模型或奖励函数评估。我们提出了一种以查询为中心的此类工作流形式化方法，并展示了经典数据库技术，即近似查询处理（AQP）和基于代理模型（PM）的过滤，能够在不改变底层模型或管道的情况下大幅减少昂贵模型调用的数量。我们的第一种策略将工作流视为在线聚合问题：它逐步采样记录，维护带有置信区间的运行聚合估计值，并在区间稳定后提前终止，当估计值落在用户指定的误差范围内时接受该估计值。第二种策略在一小组有标签的示例上训练一个轻量级的、运行在CPU上的决策树，并用其预过滤那些结果可以高置信度预测的记录，仅将不确定的记录路由到昂贵的模型。我们在TPC-DS聚合查询以及真实的大语言模型后训练管道（包括数学推理、一般指令遵循和代码生成）上评估了这两种策略。在TPC-DS上，策略AQP在平衡分布下以10-15%的预言机调用次数达到其自适应停止点，聚合误差保持在10%以下，减少了85-90%的调用；策略PM减少了60-70%的预言机调用。在大语言模型管道上，策略AQP以20-50%的预言机调用次数达到其自适应停止点，在结构化数学和代码任务上准确率损失小于5%；由奖励模型评分的开放式指令遵循任务显示出更大但可控的减少。策略PM在结构化任务上将奖励模型评分时间最多减少了19倍，准确率损失小于10%。

## Abstract
Many modern AI workflows, ranging from LLM post-training pipelines to agentic reasoning tasks, can be expressed as declarative queries whose expensive predicate is evaluated by a large model or reward function. We propose a query-centric formulation of these workflows and show that classical database techniques, namely approximate query processing (AQP) and proxy-model (PM) based filtering, can substantially reduce the number of expensive model invocations without requiring changes to the underlying models or pipelines. Our first strategy treats the workflow as an online aggregation problem: it progressively samples records, maintains a running aggregate estimate with a confidence interval, and terminates early once the interval stabilizes, accepting the estimate when it falls within a user-specified error bound. Our second strategy trains a lightweight, CPU-resident decision tree on a small set of oracle-labeled examples and uses it to pre-filter records whose outcome can be predicted with high confidence, routing only uncertain records to the expensive model. We evaluate both strategies on TPC-DS aggregate queries and on real LLM post-training pipelines including math reasoning, general instruction following, and code generation. On TPC-DS, Strategy AQP keeps aggregate error under 10% while reaching its adaptive stopping point at 10-15% of oracle calls under balanced distributions, an 85-90% reduction, and Strategy PM reduces oracle calls by 60-70%. On LLM pipelines, Strategy AQP reaches its adaptive stopping point at 20-50% of oracle calls with less than 5% accuracy loss on the structured math and code tasks; open-ended instruction following, scored by a reward model, shows a larger but bounded reduction. Strategy PM reduces reward-model scoring time by up to 19x on structured tasks with less than 10% accuracy loss.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
现代AI工作流（如大语言模型的后训练管道、智能体推理任务）中，大量昂贵的模型调用（如大型语言模型或奖励模型）成为性能瓶颈。传统的执行方式需要为所有数据记录运行完整的AI管道，导致时间和计算成本过高。本文作者观察到，许多AI工作流可以表达为声明式SQL查询（如带`WHERE`、`ORDER BY LIMIT`或`GROUP BY`子句），从而可以从查询层面利用数据库技术优化。核心动机是：**通过查询结果的结构特性（如聚合函数的误差可容忍、排序后只需要前k个等），在不改变底层模型或管道的前提下，大幅减少昂贵模型的调用次数**。

### 2. 论文提出的方法论：核心思想、关键技术细节
本文提出两种互补的策略：

#### 策略AQP（近似查询处理）
- **核心思想**：将工作流视为在线聚合问题。通过渐进均匀随机采样记录，维护聚合估计值（均值、和等）及其置信区间，在区间稳定且用户指定误差范围内提前终止调用。
- **关键技术细节**：
  - 采用自助法采样，每次抽取1%的记录，计算估计值 \(\hat{Y}\) 和样本标准差 \(s\)，置信区间半宽为 \(Z_\alpha \cdot s / \sqrt{n}\)。
  - 停止准则：当半宽不再显著下降（稳定）且小于用户误差界 \(\epsilon\) 时接受结果；否则若稳定后仍不达标则不适合AQP。
  - **LIMIT查询的扩展**：针对`ORDER BY score DESC LIMIT k`，使用顺序统计量推导概率公式：随机抽样 \(m\) 个候选包含全部全局前k的概率为 \(P_k(m,N) = \prod_{i=0}^{k-1} \frac{m-i}{N-i}\)。保守停止规则为 \(P_k(m,N) \geq \alpha\)；自适应停止规则监控第k高得分的变化，当其稳定时停止。

#### 策略PM（代理模型过滤）
- **核心思想**：训练一个轻量级、运行在CPU上的决策树代理模型，用表面特征（响应长度、符号密度、语法结构等）预测奖励模型得分。通过级联门控机制，只将低置信度的记录送往昂贵模型，其余由代理处理或跳过。
- **关键技术细节**：
  - 特征工程：从每个候选响应中提取结构/词汇/数值特征（如公式密度、代码语法标志），计算时间远小于一次模型调用。
  - 级联门控由两个检测组成：
    - **方差门**（组级跳过）：计算代理预测得分的组内标准差 \(\sigma_{\text{pred}}\)，若低于阈值 \(\sigma_{\text{th}}\) 则整个提示的候选被视为无差别，直接返回一个随机候选，零次RM调用。
    - **候选过滤器**（组内剪枝）：保留代理得分高于阈值 \(\tau\) 的记录，仅将幸存集合送入RM进行最终排名。
  - 阈值从训练样本的预测分布中自动校准（使用分位数），且预设两种模式：过滤模式（仅候选过滤器）用于代理判别力弱的情况；门控+过滤模式用于强生成器、结构化任务。
  - **回退机制**：当代理置信度低（如幸存集为空）时，退回调用全量RM以保证正确性。

#### 策略选择规则
- 根据查询结构（是否有聚合）、置信区间宽度、最小样本量（5-10%）综合判断。无聚合时默认PM；AQP的CI窄则优先AQP；否则切换或结合PM。

### 3. 实验设计：数据集/场景、benchmark、对比方法
#### 数据集与场景
- **TPC-DS**：选择8个代表查询（Q7,13,50,52,53,54,55,56），覆盖SUM、AVG、COUNT。将原始WHERE谓词分为可预测部分和结构部分，构建带二元标签的表。模拟三种标签分布：真实分布、调整为30%和50%正向率。
- **LLM后训练管道**：三个领域：
  - **数学推理**：GSM8K
  - **通用指令遵循**：UltraFeedback（无正确答案，用UltraRM-13B奖励分数度量质量）
  - **代码生成**：HumanEval+ 和 MBPP 联合
  - 每个提示生成 \(N=32\) 个候选响应，使用Skywork-Reward-V2-Qwen3-8B（数学/代码）或UltraRM-13B（通用）作为RM。
  - 生成器模型：Qwen3-8B、LLaMA-3.1-8B、Mistral-7B-V0.3、Qwen3-32B（4种规模）。

#### 对比方法
- **Full-RM**：在所有N个候选上调用RM（全基线）
- **均匀随机采样**：不同比例的随机子集
- **策略AQP**（自适应停止）
- **策略PM**（代理模型级联）
- 在TPC-DS中，还比较了两种策略在不同训练样本比例下的表现。

### 4. 资源与算力
文中明确提到：
- Qwen3-32B模型训练在 **8×A100 80GB GPU**上运行。
- 其他三个模型（Qwen3-8B、LLaMA-3.1-8B、Mistral-7B-V0.3）在 **单张NVIDIA RTX 5090**上运行。
- 训练时长未明确报告，但给出了完整的RM评分时间（秒）用于对比（如LLaMA-3.1-8B数学任务全RM需1079s，PM仅56s）。
- 论文未提及其他算力资源（如CPU型号）或数据存储细节。

### 5. 实验数量与充分性
- **实验数量丰富**：TPC-DS上8个查询×3种标签分布×10次重复，共240组基础实验；还有不同策略（AQP、PM）在不同采样率下的变化图。LLM实验包括3个领域×4个生成器×2种策略+全基线，共12组主要对比，每组报告精度/奖赏分数及RM调用时间。
- **消融分析**：在TPC-DS上比较了自然分布与调整分布，展示了AQP的局限性（小表或稀疏标签效果差）；在LLM中比较了两种预设模式（A/B）的效果，分析了不同域的优势。
- **客观性**：实验设计覆盖了多种任务、模型规模和数据分布，且报告了多次重复的误差棒（25-75百分位），结果可信。但未进行统计显著性检验（如t-检验），且没有与最新的类似工作（如Speculative Rejection、CORE等）直接对比，可能影响公平性。

### 6. 论文的主要结论与发现
- **AQP策略**：在TPC-DS平衡分布下，10-15%的预言机调用即可达到<10%聚合误差（85-90%调用减少）；在LLM结构化任务（数学、代码）中以20-50%调用保持5%以内准确率损失；通用任务上损失在~15%内。
- **PM策略**：在TPC-DS自然分布下，5/8查询在10-20%训练样本下误差<10%，总调用减少60-70%；在LLM结构化任务上最多减少19倍RM评分时间（LLaMA-3.1-8B数学：56s vs 1079s），精度损失<10%；通用任务上提升有限（2-3倍）。
- **互补性**：AQP提供可靠的一般性减少，PM在结构可预测的领域提供额外收益；两种策略可独立或结合使用。
- **查询中心视角的有效性**：暴露了管道视角看不见的优化机会（聚合函数、LIMIT、结构谓词）。

### 7. 优点
- **创新视角**：将AI工作流表达为声明式查询，使得经典数据库技术可以直接应用，无需修改模型或管道。
- **轻量高效**：AQP基于采样，PM决策树训练快速且运行在CPU，额外开销极小。
- **实验覆盖全面**：包括经典数据库基准（TPC-DS）和真实LLM任务，涵盖多种模型规模和领域，结果可信。
- **自适应机制**：自动停止准则和级联门控阈值校准避免了人为设置参数，提高了鲁棒性。
- **结构谓词特征工程**：利用响应长度、符号密度等表面特征，这些特征在推理基代理模型中不可见，是查询中心视角带来的独特优势。

### 8. 不足与局限
- **AQP的局限性**：在标签分布极度稀疏或表很小时（TPC-DS Q13、Q54），置信区间无法稳定收敛，需要切换到PM或全扫描，文中未提供更精细的切换策略。
- **PM依赖表面特征**：在自由文本指令遵循任务中，表面特征的判别力弱，PM提升有限（仅2-3倍），而通用任务其实是更主流的LLM使用场景。
- **未覆盖复杂工作流**：仅验证了后训练管道（LIMIT查询）和简单聚合，未涉及多步智能体推理（如带JOIN的查询）、检索增强生成（RAG）等更复杂的AI工作流。
- **未与最新方法对比**：未与直接优化生成侧的Speculative Rejection或管道重写系统（如DocETL、Abacus）进行系统比较，结论仅相对于全RM和随机采样。
- **统计显著性缺失**：虽然报告了误差范围，但未进行假设检验评估方法间的差异是否显著。
- **可重复性**：仅给出了代码仓库链接，但未明确说明超参数（如置信水平、阈值分位数）的标准配置，可能影响复现。

（完）
