---
title: "PersistentKV: Page-Aware Decode Scheduling for Long-Context LLM Serving on Commodity GPUs"
title_zh: PersistentKV：面向长上下文LLM在商用GPU上服务的页面感知解码调度
authors: Muhammad Ahmed
date: 2026-06-25
pdf: "https://arxiv.org/pdf/2606.26666v2"
tags: ["query:ai-infra"]
score: 10.0
evidence: 面向长上下文大语言模型服务的页面感知解码调度与KV缓存优化
tldr: 长上下文LLM服务受限于KV缓存移动而非计算，现有paged-attention和优化kernel在低活跃长上下文解码时利用率不足。PersistentKV提出页面感知解码调度，通过KV-head分组和紧凑工作队列，仅执行非空任务，直接操作页表。在RTX 3060上，结合成本模型动态选择FlashInfer或PersistentKV调度，使吞吐量提升最高1.40倍，并减少启动扇出。该方法证明了工作分配是服务系统的关键优化变量。
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.26666v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 853, \"height\": 301, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26666v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1627, \"height\": 302, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26666v2/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1373, \"height\": 243, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26666v2/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1645, \"height\": 262, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26666v2/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1138, \"height\": 223, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26666v2/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1557, \"height\": 422, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26666v2/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1054, \"height\": 183, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26666v2/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1075, \"height\": 224, \"label\": \"Table\"}]"
motivation: 长上下文LLM解码中KV缓存移动成为瓶颈，现有优化kernel在低活跃场景下GPU利用率低，混合序列长度导致调度效率矛盾。
method: 提出PersistentKV，基于KV-head分组原生页表解码注意力引擎，使用紧凑工作队列调度仅执行非空行-KV-head-序列切分任务。
result: 在RTX 3060上，成本模型策略使B8多模态负载吞吐量提升1.04-1.08倍，B1负载提升1.40倍，启动扇出从16降至2。
conclusion: 工作分配策略对长上下文LLM服务性能至关重要，PersistentKV通过页面感知调度有效提升吞吐量并减少开销。
---

## 摘要
自回归大语言模型（LLM）服务日益受到键值（KV）缓存移动而非稠密矩阵乘法的限制。现代分页注意力系统减少了碎片化，而成熟的核函数（如FlashInfer）提供了高度优化的解码注意力。然而，最佳的单核函数实现并不总是最佳的服务调度：低活跃度的长上下文解码可能使GPU利用率不足，而混合序列长度在许多精确长度启动和粗粒度填充批处理之间引入了矛盾。我们提出了PersistentKV，一种原生的块表解码注意力引擎和针对分组查询注意力（GQA）的页面感知调度研究。PersistentKV按KV头组映射工作，直接在原生页表上执行，并添加了一个紧凑的工作队列调度，仅执行非空的行-KV头-序列分割任务。在RTX 3060（FP16，页大小16，Hq=32，Hkv=8，d=128）上，经过校准的屋顶线风格策略为小型活跃批处理选择FlashInfer，为批大小为1（B1）的长上下文步骤选择PersistentKV序列分割，为支持的B8长上下文GQA步骤选择PersistentKV工作队列调度。在成本模型常数固定于校准轨迹的情况下，五个保留种子在B8双峰、均匀和类Zipf工作负载上将平均墙钟解码令牌吞吐量提高了1.04倍至1.08倍，在B1分桶轨迹上提高了1.40倍。对于B4边界情况和未校准的GQA比率，该策略通过路由到FlashInfer避免了性能退化。我们还报告了一个注意力加MLP定时代理和工作负载计数器，显示工作队列调度将保留双峰B8的每步启动扇出从16.00减少到2.00。这些结果表明，工作分配是一个决定性的服务系统变量。

## Abstract
Autoregressive large language model (LLM) serving is increasingly limited by key-value (KV) cache movement rather than dense matrix multiplication. Modern paged-attention systems reduce fragmentation, and mature kernels like FlashInfer provide highly optimized decode attention. However, the best single-kernel implementation is not always the best serving schedule: low-active long-context decode can under-utilize GPUs, while mixed sequence lengths introduce tension between many exact-length launches and coarse padded batches.   We present PersistentKV, a native block-table decode attention engine and page-aware scheduling study for grouped-query attention (GQA). PersistentKV maps work by KV-head group, executes directly over native page tables, and adds a compact workqueue schedule executing only non-empty row-KV-head-sequence-split tasks.   On an RTX 3060 (FP16, page size 16, Hq=32, Hkv=8, d=128), a calibrated roofline-style policy selects FlashInfer for small active batches, PersistentKV sequence splitting for batch size 1 (B1) long-context steps, and PersistentKV workqueue scheduling for supported B8 long-context GQA steps. With cost-model constants fixed on calibration traces, five held-out seeds improve mean wall decode-token throughput by 1.04x to 1.08x on B8 bimodal, uniform, and Zipf-like workloads, and by 1.40x on a B1 bucketed trace. For the B4 boundary case and uncalibrated GQA ratios, the policy avoids regressions by routing to FlashInfer. We also report an attention-plus-MLP timing proxy and workload counters showing workqueue scheduling reduces launch fan-out from 16.00 to 2.00 launches per step on held-out bimodal B8. These results show that work assignment is a decisive serving-system variable.

---

## 论文详细总结（自动生成）

# 论文总结：PersistentKV: Page-Aware Decode Scheduling for Long-Context LLM Serving on Commodity GPUs

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：长上下文大语言模型（LLM）服务中，解码阶段的性能瓶颈已从稠密矩阵乘法转向KV缓存移动。现有分页注意力系统（如FlashInfer）虽然提供了高度优化的内核，但在低活跃度（batch size很小）的长上下文解码场景下，GPU利用率不足；混合序列长度导致精确长度启动与粗粒度填充批处理之间的调度矛盾。
- **整体含义**：当前最优的单内核实现并不总是最佳服务调度方案。工作分配（work assignment）而非仅仅注意力计算，是决定服务系统性能的关键变量。论文旨在通过页面感知的解码调度，在商用GPU上改善长上下文LLM服务的吞吐量。

## 2. 论文提出的方法论

- **核心思想**：设计一个原生块表（native block-table）解码注意力引擎——PersistentKV，它按KV头组（KV-head group）分配工作，直接在原生页表上执行，避免将KV缓存重排成连续张量。通过引入紧凑工作队列（compact workqueue）调度，仅执行非空的行-头-序列切分任务，减少启动扇出和填充浪费。
- **关键技术细节**：
  - **序列切分与行局部边界**：将每个序列的KV维度切分为多个范围（split），每个split计算部分online-softmax状态，再通过合并内核精确合并。使用行级有效长度（row-local effective length）推导tile计数、split范围等，避免填充读取。
  - **紧凑工作队列调度**：对每个活跃行、KV头、行局部split，仅当tile区间非空时发射任务。CUDA网格为一维任务列表，合并内核完成分段softmax约简。避免了长度桶调度造成的多启动扇出。
  - **校准成本模型**：基于roofline风格策略，估计每个候选路线（FlashInfer、PersistentKV桶、工作队列）的耗时，考虑带宽、CTA工作数、启动开销等。硬性门槛：仅对校准的G=4 GQA启用PersistentKV；短上下文（<16K tokens）路由到FlashInfer；B4边界情况默认使用FlashInfer以避免回归。
- **公式/算法流程**（文字说明）：
  - 解码注意力公式标准，GQA中多个查询头共享同一KV头。
  - 每个split计算局部最大值(m_s)、指数和(l_s)、加权和(a_s)，合并阶段取全局最大值，组合得到最终输出。
  - 成本模型公式：估计时间 = KV字节/带宽 × max(1, 最小占用/当前CTA工作) + 启动次数×启动开销 + 合并字节/带宽。

## 3. 实验设计

- **数据集/场景**：使用合成轨迹（trace），包括：
  - 分桶轨迹（bucketed trace）：prompt长度循环为8K、16K、32K、64K，模拟长上下文交互式会话。
  - 同质轨迹（homogeneous trace）：统一32K prompt。
  - 双峰轨迹（bimodal trace）：长短prompt混合，每三个短请求配一个长请求。
  - 均匀轨迹（uniform trace）和Zipf轨迹：测试广泛及重尾长度分布。
  - 外部可重分发的CSV轨迹夹具（32个请求，最大活跃8）。
- **Benchmark**：对比方法包括FlashInfer 0.2.5（主要基线）、vLLM 0.6.4.post1、TensorRT-LLM 0.8 MMHA、以及“重排+PyTorch SDPA”框架基线。
- **指标**：解码令牌吞吐量（decode-token throughput），包括CUDA事件时间（设备工作）和同步墙钟时间（端到端延迟）。此外报告了调度器计数的结构指标（启动数/步、合并流量等），以及Nsight Compute硬件计数器。

## 4. 资源与算力

- **GPU型号**：单张NVIDIA RTX 3060（12 GB VRAM，28个SM，CUDA 12.1，PyTorch 2.5.1）。
- **数量**：单GPU实验。
- **训练时长**：论文未涉及训练，仅推理服务。未明确提及总运行时长。
- **备注**：作者指出其他GPU（如A100、H100）未测试，需要重新校准成本常数。

## 5. 实验数量与充分性

- **实验数量**：
  - 孤立原生页表基线测试（单个请求，3种长度×4种方法）。
  - 主要服务结果：5个保留种子（20260623-20260627）上的校准策略对比，报告了B1分桶、B8双峰/均匀/Zipf、B4边界共5种轨迹。
  - 模型级代理定时（注意力+MLP尾）：5个种子B8双峰。
  - 调度器结构计数器（1个双峰B8轨迹）。
  - Nsight Compute硬件计数器（短双峰B8轨迹）。
  - 路线和分裂校准（分裂数敏感性、B4工作队列扫描）。
  - 随机性消融（比较旧长度桶与工作队列）。
  - 实现消融（索引Q/O、行局部边界、零tile提前退出、紧凑工作队列）。
  - GQA/MQA扫描（B8双峰下G=1,4,8）。
  - 外部轨迹夹具测试（1个轨迹）。
- **充分性评价**：实验设计较为充分，覆盖了多种合成轨迹、不同批量大小、分裂数敏感性、实现变体消融、硬件计数器证据。但局限性在于：全部使用合成轨迹，未使用真实生产流量；仅一组GPU；对比基线版本固定；B8增益较小（1.04-1.08x），需更多验证。作者在威胁有效性中承认了这些局限。

## 6. 论文的主要结论与发现

- **主要结论**：页面感知的解码调度（PersistentKV）在特定长上下文服务场景下可超越强基线FlashInfer，尤其适用于低活跃（B1）和中等活跃（B8）长上下文解码。
- **具体发现**：
  - B1分桶轨迹：墙钟吞吐量提升1.40倍（±0.065）。
  - B8双峰、均匀、Zipf轨迹：提升1.044-1.080倍。
  - 注意力+MLP代理：B8双峰提升1.105倍。
  - 紧凑工作队列将启动扇出从16.00降到2.00，合并流量减少。
  - B4边界情况和未校准GQA比率，策略路由到FlashInfer避免回归。
- **系统级含义**：工作分配（work assignment）是决定服务性能的关键变量，不应只追求单一内核优化。

## 7. 优点

- **方法创新**：首次将页面感知调度与原生块表执行结合，提出紧凑工作队列避免多启动扇出，在低活跃长上下文场景中有效。
- **实验严谨**：使用相同页面布局、GQA形状、上下文长度和数值容差进行公平对比；包含校准成本模型和保留种子验证；提供了NVIDIA Nsight Compute硬件计数器支持因果分析。
- **可复现性**：发布了校准常数、合成轨迹生成器和可重分发的CSV轨迹夹具，便于复现和扩展。
- **清晰的结构**：明确区分了不同批量大小（B1/B4/B8）的最佳路线，并显示了校准策略避免回归的行为。

## 8. 不足与局限

- **实验覆盖不足**：
  - 仅使用合成轨迹，未使用真实生产流量，缺乏预填充/解码干扰、网络延迟、内存压力等现实因素。
  - 仅单GPU平台（RTX 3060），未在A100、H100等数据中心GPU上验证，通用性存疑。
  - 对比基线版本固定（FlashInfer 0.2.5等），未来版本可能改变性能平衡。
- **偏差风险**：
  - B8增益较小（1.04-1.08x），可能受测量噪声影响；作者未提供置信区间宽度统计。
  - 校准常数基于一个种子，保留种子虽5个但仍是合成数据，可能过拟合。
- **应用限制**：
  - PersistentKV当前仅校准了G=4 GQA；G=1和G=8被路由到FlashInfer，未提供原生支持。
  - 未集成到完整服务运行时（如vLLM、TensorRT-LLM），仅模拟解码循环。
  - 模型级代理仅包含单层注意力+MLP，未评估完整变压器堆栈。
  - 短上下文（<16K tokens）仍路由到FlashInfer，未能覆盖所有长上下文场景。
- **性能边界**：B4工作队列接近等价但未稳定超越，说明合并/启动开销在中等批量时仍构成瓶颈。

（完）
