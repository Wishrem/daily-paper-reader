---
title: Adaptive Inference Batching using Policy Gradients
title_zh: 使用策略梯度的自适应推理批处理
authors: Ruslan Sharifullin
date: 2026-07-06
pdf: "https://arxiv.org/pdf/2607.05272v1"
tags: ["query:ai-infra"]
score: 8.0
evidence: 推理服务批处理策略
tldr: "推理服务面临突发异构负载，静态批处理策略需手动调优且无法适应。提出基于强化学习的自适应批处理和路由策略，在模拟器上训练REINFORCE和PPO智能体。单GPU下RL增益微小，多GPU异构路由中RL发现负载分离策略消除头部阻塞，吞吐量提升3.5倍，延迟降低25%。表明RL优势集中于多资源组合决策，为生产环境选择学习策略提供边界条件。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.05272v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 651, \"height\": 370, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05272v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 703, \"height\": 599, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05272v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 615, \"height\": 391, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05272v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 611, \"height\": 404, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05272v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 605, \"height\": 503, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.05272v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 710, \"height\": 138, \"label\": \"Table\"}]"
motivation: 静态批处理策略无法适应突发异构负载，需要自适应推理调度。
method: 将问题建模为MDP，用REINFORCE和PPO在离散事件模拟器上训练智能体。
result: "多GPU环境下RL策略实现3.5倍吞吐提升和25%延迟降低，优于最强启发式基线48%。"
conclusion: RL在单资源调度中增益有限，在多资源组合决策中显著优于启发式。
---

## 摘要
推理服务系统必须在突发性、异构工作负载下平衡吞吐量和延迟，然而行业标准仍然是静态批处理策略，需要手动调优且无法适应不断变化的流量。我们研究强化学习（RL）能否学习到优于这些启发式方法的自适应批处理和路由策略，在基于离散事件模拟器上训练REINFORCE和PPO智能体，该模拟器经过排队理论和生产跟踪数据（Azure Functions、BurstGPT）验证。我们将问题建模为关于队列状态、请求类型和GPU可用性的马尔可夫决策过程（MDP），并在标准泊松流量、极端突发、真实世界跟踪以及异构多GPU路由上进行评估。我们的核心发现是RL在系统问题中价值的明确边界条件。在单GPU设置中，精心调整的静态批处理策略在类似泊松到达下已接近最优，RL仅带来边际增益（+0.1%至+1.0%）。然而，在多GPU异构路由中，快速和慢速请求竞争共享资源，智能体发现了一种工作负载分离策略，消除了队头阻塞，相比轮询（Round-Robin）提升3.5倍（348%），相比最强的启发式基线（最短队列，Shortest-Queue）提升48%，在满足服务水平协议（SLA）约束的同时，吞吐量提高60%，延迟降低25%。尽管仅使用合成泊松到达进行训练，该策略仍能泛化到未见过的突发和真实世界流量，并且注意力增强的策略网络收敛速度比多层感知机（MLP）基线快约20%。这些结果表明RL相对于工程启发式方法的优势集中在组合性、多资源决策上，而非单资源的时间调度，这对于在生产推理基础设施中决定哪里使用学习策略以证明其工程成本具有实际指导意义。

## Abstract
Inference serving systems must balance throughput and latency under bursty, heterogeneous workloads, yet the industry standard remains static batching policies that require manual tuning and cannot adapt to shifting traffic. We investigate whether reinforcement learning (RL) can learn adaptive batching and routing policies that outperform these heuristics, training REINFORCE and PPO agents on a discrete-event simulator validated against queuing theory and production traces (Azure Functions, BurstGPT). We formulate the problem as an MDP over queue state, request type and GPU availability, evaluating across standard Poisson traffic, extreme bursts, real-world traces and heterogeneous multi-GPU routing.   Our central finding is a clear boundary condition for RL's value in systems problems. In single-GPU settings, a well-tuned static batching policy is already near-optimal under Poisson-like arrivals and RL offers only marginal gains (+0.1% to +1.0%). In multi-GPU heterogeneous routing, however, where fast and slow requests compete for shared resources, the agent discovers a workload-segregation policy that eliminates Head-of-Line blocking, yielding a 3.5x (348%) improvement over Round-Robin and a 48% improvement over the strongest heuristic baseline (Shortest-Queue), with 60% higher throughput and 25% lower latency while respecting SLA constraints. The policy generalizes to unseen bursty and real-world traffic despite training only on synthetic Poisson arrivals and an attention-augmented policy network converges roughly 20% faster than an MLP baseline.   These results suggest RL's advantage over engineered heuristics concentrates in combinatorial, multi-resource decisions rather than single-resource temporal scheduling, a practical distinction for deciding where learned policies justify their engineering cost in production inference infrastructure.