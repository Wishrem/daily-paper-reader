---
title: "PersistentKV: Page-Aware Decode Scheduling for Long-Context LLM Serving on Commodity GPUs"
title_zh: PersistentKV：面向商品GPU长上下文LLM服务的页面感知解码调度
authors: Muhammad Ahmed
date: 2026-06-25
pdf: "https://arxiv.org/pdf/2606.26666v1"
tags: ["query:ai-infra"]
score: 9.0
evidence: 针对长上下文LLM的KV缓存解码调度
tldr: 长上下文LLM服务中KV缓存移动成为瓶颈，现有分页注意力系统存在低利用率问题。PersistentKV通过页面感知解码调度，以KV头组映射、K/V瓦片重用及紧凑工作队列调度提升GPU利用率。在RTX 3060上，自适应策略在多种负载下实现1.063-1.399x吞吐提升，表明工作分配是决定性系统变量。
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.26666v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 854, \"height\": 300, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26666v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1461, \"height\": 304, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26666v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1645, \"height\": 265, \"label\": \"Table\"}]"
motivation: 长上下文LLM服务受限于KV缓存移动而非计算，现有分页注意力系统在低活跃场景下GPU利用率不足。
method: PersistentKV采用本机块表解码注意力，通过KV头组映射重用K/V瓦片，并引入紧凑工作队列调度执行非空任务。
result: 在RTX 3060上，自适应策略在单批与八批长上下文步骤中分别提升同步吞吐1.399x和1.063-1.265x。
conclusion: 页面感知解码调度通过优化工作分配而非仅注意力计算，成为提升长上下文LLM服务性能的关键。
---

## 摘要
自回归大语言模型（LLM）服务日益受到键值（KV）缓存移动而非稠密矩阵乘法的限制。现代分页注意力系统减少了KV缓存碎片，而成熟的核函数（如FlashInfer）提供了高度优化的原生分页解码注意力。然而，最佳的单核实现并非总是最优的服务调度：低活跃度的长上下文解码可能无法充分利用商品GPU，而混合序列长度则在多次精确长度启动与粗粒度填充批处理之间引入矛盾。我们提出PersistentKV，一个原生块表解码注意力引擎和针对分组查询注意力（GQA）的页面感知调度研究。PersistentKV按KV头组映射工作，旨在跨分组查询头重用K、V分块，支持原生页表，并添加了紧凑的工作队列调度，仅执行非空的行-KV头-序列拆分任务。在RTX 3060上，采用FP16、页大小16、Hq=32、Hkv=8、d=128，且与FlashInfer保持相同的正确性容差，一个校准的自适应策略为小活跃批次选择FlashInfer，为B1长上下文步骤选择PersistentKV序列拆分，为B8长上下文步骤选择PersistentKV工作队列调度。在校准轨迹上固定阈值和拆分计数，一个保留的轨迹种子在B8双峰、均匀和Zipf-like工作负载上实现了1.063-1.265倍的同步墙时间吞吐提升，在B1分桶轨迹上实现了1.399倍提升。在B4双峰边界情况下，该策略通过选择FlashInfer避免了PersistentKV的回归。这些结果确定了自适应页面感知解码调度的具体系统场景，并表明工作分配（而非仅注意力数学）是决定服务系统性能的关键变量。

## Abstract
Autoregressive large language model (LLM) serving is increasingly limited by key-value (KV) cache movement rather than dense matrix multiplication. Modern paged-attention systems reduce KV-cache fragmentation and mature kernels such as FlashInfer provide highly optimized native-paged decode attention. However, the best single-kernel implementation is not always the best serving schedule: low-active long-context decode can under-utilize commodity GPUs, while mixed sequence lengths introduce a tension between many exact-length launches and coarse padded batches. We present PersistentKV, a native block-table decode attention engine and page-aware scheduling study for grouped-query attention (GQA). PersistentKV maps work by KV-head group, is designed to reuse K,V tiles across grouped query heads, supports native page tables, and adds a compact workqueue schedule that executes only non-empty row-KV-head-sequence-split tasks. On an RTX 3060 with FP16, page size 16, Hq=32, Hkv=8, d=128, and identical correctness tolerance against FlashInfer, a calibrated adaptive policy selects FlashInfer for small active batches, PersistentKV sequence splitting for B1 long-context steps, and PersistentKV workqueue scheduling for B8 long-context steps. With thresholds and split counts fixed on calibration traces, one held-out trace seed improves synchronized wall throughput by 1.063-1.265x on B8 bimodal, uniform, and Zipf-like workloads and by 1.399x on a B1 bucketed trace. On the B4 bimodal boundary case, the policy avoids the PersistentKV regression by selecting FlashInfer. These results identify a concrete systems niche for adaptive page-aware decode scheduling and show that work assignment, not only attention math, is a decisive serving-system variable.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：长上下文 LLM 服务中，自回归解码阶段的性能瓶颈已从稠密矩阵乘法转变为 **KV 缓存移动**。现代分页注意力系统（如 PagedAttention）虽减少了缓存碎片，但现有高度优化的单核实现（如 FlashInfer）在低活跃度（小批量长上下文）或高度参差不齐的序列长度混合场景下，无法充分利用商品 GPU 的并行能力，导致批处理调度低效（过多精确长度启动或粗粒度填充浪费）。
- **整体含义**：论文指出，**工作分配**（调度策略）而非仅仅注意力的数学计算，是决定服务系统性能的关键变量。需要针对页面布局和请求状态进行自适应调度，在特定工况下超越最强基线。

## 2. 方法论：核心思想、关键技术细节

### 2.1 核心思想
提出 **PersistentKV**，一个原生块表（native block-table）解码注意力引擎，结合页面感知调度，实现：
- 按 KV 头组（GQA）映射工作，重用 K/V 分块；
- 序列分割暴露并行度；
- 紧凑工作队列仅执行非空任务，避免启动扇出和填充浪费；
- 最终通过**校准的自适应策略**在不同请求状态下选择最优调度路径。

### 2.2 关键技术细节

- **原生块表执行**：KV 页存储为 `K,V[物理页, Hkv, 页大小, d]`，请求通过块表映射逻辑页到物理页，无需 repack。采用 32-token 分块、异步拷贝、精确在线 softmax 状态（FP32）。
- **序列分割（Sequence Splitting）**：将序列分成 S 个不相交区间，每个区间计算部分 softmax 状态，然后合并。采用**逐行有效长度**（而非桶长度）确定分块边界、瓦片数量等，空分割直接写中立状态提前返回，避免填充 KV 读取。
- **紧凑工作队列调度（Compact Workqueue）**：对每个活跃行、KV 头、行局部分割，仅当瓦片区间非空时发射一个任务。CUDA 网格是一维任务列表，每个任务计算部分状态并存储到紧凑槽位，再用合并核进行分段 softmax 归约。以此将一次解码步骤的 CUDA 启动次数从精确分桶时的 16 次降低到 2 次。
- **自适应策略**：手动校准的阈值策略：
  - B≤1：PersistentKV 长度桶（序列分割）；
  - B≥8 且 Nmax≥16K：PersistentKV 工作队列；
  - 否则：FlashInfer。
  阈值和分割计数在校准种子（20260622）上选定，在保留种子（20260623）上报告。

## 3. 实验设计

### 3.1 数据集/场景
使用**合成轨迹**，以控制请求结构、隔离调度失败模式：
- **分桶轨迹（Bucketed）**：提示长度循环 8K/16K/32K/64K，模拟固定上下文窗口的应用。
- **同质轨迹（Homogeneous）**：通用 32K 提示长度，独立解码长度，隔离批量大小影响。
- **双峰轨迹（Bimodal）**：每个长请求配三个短请求，对长度分桶调度具有敌意。
- **均匀轨迹（Uniform）**和**Zipf轨迹**：提供广度分布和重尾分布压力测试。

### 3.2 Benchmark
- **孤立原生分页基线**：单请求注意力测试，对比 FlashInfer、vLLM、TensorRT-LLM MMHA、Repack+PyTorch SDPA。
- **主要服务结果**：在保留种子上，自适应策略 vs FlashInfer 在 B1、B8、B4 模式下的同步墙时间吞吐和 GPU 时间提升。
- **消融实验**：分段粒度、工作队列 vs 精确分桶、融合合并、CUDA 图回放、索引 Q/O、行局部边界、零瓦片提前退出等。

### 3.3 对比方法
- 基线：FlashInfer 0.2.5（主要基线）、vLLM 0.6.4.post1、TensorRT-LLM 0.8 MMHA、Repack+PyTorch SDPA。
- 对比条件：相同 GQA 形状（Hq=32, Hkv=8, G=4, d=128）、FP16、页大小 16、相同物理页分配、相同正确性容差（max|e|<2e-3, mean<3e-4）。

## 4. 资源与算力

- **GPU**：单块 NVIDIA RTX 3060（12 GB VRAM），28 个 SM，流式读取带宽约 302 GB/s。
- **CUDA 版本**：12.1，PyTorch 2.5.1。
- **训练时长**：论文未提及训练，仅涉及推理服务性能测量。实验重复次数未明确说明，但使用校准/保留种子协议，应有一定统计意义。
- **未明确说明**：未提及使用的 CPU 型号、内存、网络等；未提供多卡或更大规模 GPU 的实验。

## 5. 实验数量与充分性

### 5.1 实验数量
- 孤立基线测试：1 组表格（5 个方法×3 种长度）。
- 主要服务结果：1 组表格（5 条轨迹×两种比率）。
- 调度器计数器：1 组表格（4 种引擎的启动次数、合并流量等）。
- 消融实验：多个（分段粒度、工作队列、融合合并、CUDA 图、索引 Q/O、行局部边界、零瓦片提前退出），但未全部以表格形式呈现。
- 总计约 10 组左右核心实验。

### 5.2 充分性与公平性评价
- **充分性**：
  - 覆盖了低活跃（B1）、中等活跃（B4）、高活跃（B8）三种批量规模；
  - 使用了多种合成轨迹（双峰、均匀、Zipf、分桶）以模拟不同长度分布；
  - 进行了校准/保留种子分离，避免过拟合阈值；
  - 进行了多项消融，分析了关键设计决策的影响。
- **客观性**：
  - 对比条件完全一致（相同页表、形状、容忍度、FP16）；
  - 报告了 GPU 时间和墙时间两种视角；
  - 明确指出某些配置效果差（如 B4 回归、融合合并更慢）。
- **不足**：
  - 仅使用一个保留种子，未报告均值和方差，统计可靠性有限；
  - 合成轨迹不能完全代表生产流量；
  - 仅单 GPU 实验，未验证在不同架构（如 Hopper）上的表现；
  - 基线版本固定，可能随时间过时。

## 6. 主要结论与发现

- **PersistentKV 在低活跃长上下文解码（B1）上**：通过序列分割暴露更多 CTA 并行度，在保留种子上实现 **1.399×** 墙时间吞吐提升。
- **工作队列调度在 B8 重负载场景**：通过减少启动次数（16→2）和合并流量，在双峰、均匀、Zipf 轨迹上实现 **1.063–1.265×** 墙时间吞吐提升。
- **在 B4 边界情况下**：自适应策略选择 FlashInfer 避免回归（PersistentKV 因合并启动开销过大而变慢）。
- **核心洞察**：工作分配（调度）是决定性系统变量，单一最优核并不总能带来最优服务性能。

## 7. 优点

1. **问题聚焦新颖**：将注意力从“更快注意力核”转向“页面感知调度”，揭示了一个被忽视的性能维度。
2. **方法论严谨**：
   - 采用校准/保留种子协议，避免阈值过拟合；
   - 明确报告 GPU 时间和墙时间，隔离主机开销；
   - 提供调度器导出的结构计数器（启动次数、合并流量），便于理解性能来源。
3. **消融实验全面**：逐一剥离关键技术（索引 Q/O、行局部边界、零瓦片退出、工作队列、融合合并），量化每个贡献的效果。
4. **可复现性**：使用公开的 FlashInfer、vLLM 等作为基线，且严格保持相同页表、形状、数值容忍度。

## 8. 不足与局限

1. **实验覆盖不足**：
   - 仅使用 RTX 3060（28 SM 的消费级 GPU），未在数据中心级 GPU（如 A100/H100）上验证；
   - 仅一个保留种子，缺乏统计显著性（应至少 5 个种子报告均值和置信区间）；
   - 未集成到完整服务系统（如 vLLM、TensorRT-LLM），仅测试了解码循环，缺少 MLP、采样、网络、请求队列等实际组件；
   - 仅测试 G=4 的 GQA 形状，未验证其他分组比率。
2. **合成轨迹风险**：轨迹是人工设计的，不包含生产系统中常见的访问模式、波动和干扰。
3. **硬件性能计数器受限**：因权限限制了 Nsight Compute，无法报告 L2 命中率、DRAM 字节、SM 占用率等硬件指标，削弱了性能根因分析。
4. **自适应策略简单**：手动阈值，非学习式或在线优化，生产系统需更精细的成本模型。
5. **版本依赖**：FlashInfer/TensorRT-LLM/vLLM 版本固定，可能随后续优化而改变相对性能。
6. **缺少融合合并优化**：尝试的原子合并反而更慢，表明 B4 场景需要更轻量级的设计。

（完）
