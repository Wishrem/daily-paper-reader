---
title: "Grouped Query Experts: Mixture-of-Experts on GQA Self-Attention"
title_zh: 分组查询专家：GQA自注意力上的专家混合
authors: "Vishesh Tripathi, Abhay Kumar"
date: 2026-06-18
pdf: "https://arxiv.org/pdf/2606.20945v2"
tags: ["query:ai-infra"]
score: 9.0
evidence: 在分组查询注意力上应用专家混合以保持KV缓存优势
tldr: 自注意力机制是Transformer的核心，但随序列长度二次增长的计算成本成为瓶颈，且所有注意力头对所有token均匀激活导致计算浪费。提出Grouped Query Experts (GQE)，在分组查询注意力基础上，为每个GQA组引入混合专家层，由路由器为每个token选择k个查询头专家，而键值头保持密集不变。在250M参数规模、30B token预算下，GQE激活一半查询头即可匹配全激活基线在下游任务上的准确率。该方法保留了GQA的KV缓存优势，显著降低了查询头计算量。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.20945v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1554, \"height\": 659, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20945v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1067, \"height\": 349, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20945v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1163, \"height\": 888, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20945v2/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1514, \"height\": 768, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20945v2/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1501, \"height\": 766, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20945v2/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1499, \"height\": 762, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20945v2/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1503, \"height\": 762, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.20945v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1379, \"height\": 510, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.20945v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1630, \"height\": 335, \"label\": \"Table\"}]"
motivation: 标准稠密注意力对所有token均匀激活，计算效率低，尤其长序列下二次复杂度导致成本剧增。
method: 在GQA的每组查询头上，使用路由器选择k个查询头专家，键值头保持全密集，实现部分激活的查询头计算。
result: 在250M参数规模、30B token训练下，激活一半查询头即可达到与全激活GQA基线相当的准确率。
conclusion: GQE通过专家选择机制以更少计算保持模型性能，兼顾GQA的KV缓存优势与计算效率。
---

## 摘要
自注意力是Transformer性能的核心，且在长上下文长度下通常是Transformer中最昂贵的部分，因为其成对令牌交互的计算量随序列长度呈二次方增长。标准的密集注意力还会对所有令牌应用相同的注意力头，无论令牌的难度或信息内容如何。这种统一激活会浪费计算资源，尤其是当序列变长且注意力成本迅速增加时。我们提出了分组查询专家（GQE），一种在分组查询注意力（GQA）之上的专家混合层。在每个GQA组内，一个路由器为每个令牌选择k个查询头专家，而所有键值（KV）头保持密集且不变。因此，GQE保留了GQA的KV缓存优势，并仅减少了活跃的查询头计算。在250M参数规模下，固定30B令牌预算时，GQE在下游准确性上与全活跃的GQA基线相当，同时每个令牌只激活一半的查询头。

## Abstract
Self-attention is central to Transformer performance and is often the most expensive part of the Transformer at long context lengths because its pairwise token interactions scale quadratically with sequence length. Standard dense attention also applies the same set of attention heads to every token regardless of token difficulty or information content. This uniform activation can waste compute, especially as sequences grow longer and attention cost increases rapidly. We propose Grouped Query Experts (GQE), a mixture-of-experts layer on top of grouped-query attention (GQA). Within each GQA group, a router selects k query-head experts per token while all key-value (KV) heads remain dense and unchanged. Thus, GQE keeps the KV cache benefits of GQA and reduces only the active query-head computation. On a fixed 30B token budget at the 250M parameter scale, GQE matches the all-active GQA baseline in downstream accuracy while activating half the query heads per token.

---

## 论文详细总结（自动生成）

# Grouped Query Experts: 分组查询专家——GQA自注意力上的专家混合

## 论文详细中文总结

### 1. 核心问题与整体含义（研究动机和背景）
- **核心问题**：Transformer的自注意力机制计算成本随序列长度呈二次增长，且标准密集注意力对所有token均匀激活所有注意力头，导致计算浪费。尤其在长上下文场景下，注意力计算成为主要瓶颈。
- **研究动机**：探索将“条件计算”（mixture-of-experts）的思想从Transformer的MLP模块迁移到注意力模块，使得不同token可以根据自身需求激活不同的查询头，从而在不降低模型质量的前提下减少计算量。
- **整体含义**：该工作旨在提升长序列Transformer的推理效率，同时保持模型表达能力。通过保持GQA的KV缓存优势，仅减少活跃查询头计算，实现更高效的注意力机制。

### 2. 方法论：核心思想、关键技术细节
- **核心思想**：在GQA（分组查询注意力）的每组查询头上，引入一个专家混合（MoE）层。每组包含多个查询头专家，路由器为每个token选择top-k个专家进行激活，而KV头保持全密集且不变。
- **关键技术细节**：
  - **分组方式**：总查询头数 \(N\)，分为 \(G\) 组（每组对应一个KV头），每组有 \(M = N/G\) 个专家。
  - **路由机制**：对于每个token \(x_i\) 和每组 \(g\)，路由器产生softmax概率 \(p_{i,g} \in \mathbb{R}^M\)，然后选择top-k个专家。
  - **输出构建**：
    - 选中专家的输出直接硬拼接（无缩放）。
    - 增加一个“重归一化加权和”槽位：将选中专家的输出按重归一化的路由器概率加权求和，提供可微分的路由学习信号。
    - 增加一个始终激活的共享头 \(s(x_i)\)，提供稳定的注意力路径。
  - **最终输出**：拼接硬选专家输出、加权和槽位、共享头输出，通过输出投影 \(W^O\) 映射（\(W^O\) 的输入维度变为 \(kG + 2\)）。
  - **辅助损失**：使用负载平衡损失防止路由器坍塌。
- **公式/算法流程（文字描述）**：
  1. 输入 \(X \in \mathbb{R}^{n \times d}\)。
  2. 对每个token和每组，计算路由器得分并softmax。
  3. 每组选择top-k专家，计算其注意力输出。
  4. 构造输出：硬选专家输出 + 重归一化加权和 + 共享头输出。
  5. 通过投影 \(W^O\) 得到最终注意力输出。
  6. 训练时结合语言建模损失和负载平衡损失。

### 3. 实验设计：数据集、benchmark、对比方法
- **数据集**：
  - 训练数据：FineWeb-Edu（30B token子集）。
  - 下游评测：HellaSwag、PIQA、ARC-Easy。
- **Benchmark**：对比GQA基线（所有16个查询头全激活）以及中间消融变体。
- **对比方法**：
  - GQA基线（所有16头活跃）。
  - 加权拼接（无重归一化槽位）。
  - 硬拼接（仅硬选专家）。
  - GQE最终版（重归一化评分 + 共享头）。
- **实验场景**：短序列和长序列（2k到1024k token）的预填充延迟速度比较。

### 4. 资源与算力
- **显式说明**：论文未明确说明使用的GPU型号、数量或训练时长。仅提到训练在250M参数规模、30B token预算下进行，采用混合精度BFloat16，批次大小为1.05M token，序列长度2048。未提及具体硬件资源。
- **备注**：这是论文的一个信息不足点。

### 5. 实验数量与充分性
- **实验数量**：
  - 主实验：四种变体（基线、加权拼接、硬拼接、GQE最终版）在三个下游任务上的准确率（表2）。
  - 吞吐量实验：在不同序列长度（2k–1024k）下的速度对比（图1）。
  - 消融实验：仅在主配置（16头/8 KV头，k=1）上进行了路由和输出构造选择的消融。
  - 训练损失曲线和下游任务精度趋势图在附录中提供。
- **充分性评估**：
  - **优点**：消融实验明确展示了不同设计选择的效果，因果链清晰。
  - **不足**：仅使用单一种子（未提多种子平均）、单一模型规模（250M）、单一训练预算（30B token）。未在更大规模或不同架构上验证。未比较其他稀疏注意力方法（如SwitchHead、MoH等）。吞吐量实验仅在预填充阶段测量，未覆盖解码阶段（但解码阶段KV缓存是瓶颈，而GQE不节省KV缓存）。
  - **客观性**：实验设计合理，但覆盖范围有限，结论的泛化性待验证。

### 6. 主要结论与发现
- GQE在激活一半路由查询头（包括共享头后为9/16）的情况下，在下游任务平均准确率上匹配甚至略超过全激活GQA基线（56.04 vs 55.86）。
- 消融实验表明：仅硬拼接或加权拼接而不加重归一化槽位和共享头会导致准确率下降，证明路由学习信号和稳定路径的必要性。
- 长序列下（4k–1024k）预填充速度提升约1.7–1.8倍，短序列（2k）下速度提升较小（1.15倍），说明路由开销在短序列中相对较大。
- 该方法保留了GQA的KV缓存优势，仅减少查询头计算，因此与KV缓存相关的加速不显著。

### 7. 优点（方法或实验设计亮点）
- **方法创新**：将MoE应用于GQA注意力组的查询头，而非整个注意力头池，保持与原始GQA配置兼容，易于集成。
- **设计精巧**：通过重归一化加权和槽位提供可微分路由信号，同时共享头稳定训练，解决了单纯硬路由或加权路由质量下降的问题。
- **实验因果清晰**：消融实验逐步验证各组件（路由信号、共享头）的必要性，结论有说服力。
- **负载平衡损失**：防止路由器塌陷，保证专家利用率。
- **长序列速度优势明显**：针对长上下文场景设计，实际应用价值高。

### 8. 不足与局限
- **实验规模单一**：仅250M参数、30B token，未验证在更大模型（如1B+）或更多训练数据下的效果。
- **未与现有工作公平比较**：未与SwitchHead、MoH、MoMHA等稀疏注意力方法对比（虽然论文在相关工作部分提及，但实验未纳入）。
- **仅测预填充速度**：未测量解码阶段速度（GQE不减少KV缓存，解码段加速可能有限）。
- **单种子结果**：未报告多次运行的方差或统计显著性。
- **专家池较小**：每组仅2个专家（16头/8组），路由选择空间有限，更大专家池下的效果未知。
- **下游任务有限**：仅三个常识推理任务，未覆盖更广泛的语言理解或生成基准。
- **训练预算固定**：未探讨不同训练预算下GQE相对于基线的表现趋势。
- **硬件资源未披露**：影响可复现性。

（完）
