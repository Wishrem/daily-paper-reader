---
title: Cache-Resident LLM Inference in GB-Scale Last-Level Caches
title_zh: GB级末级缓存中的缓存驻留型大语言模型推理
authors: "Wanning Zhang, Tongzhou Gu, Marco Canini, Ceyu Xu, Jian Weng"
date: 2026-06-24
pdf: "https://arxiv.org/pdf/2606.25353v1"
tags: ["query:ai-infra"]
score: 10.0
evidence: 利用GB级末级缓存实现缓存驻留LLM推理，涉及KV缓存管理
tldr: 大语言模型推理受限于内存层级间的数据搬运。利用3D堆叠缓存技术的GB级末级缓存，可将可复用模型权重保留在片上。然而深度流水线导致请求和KV缓存占用增加，算子边界同步成为瓶颈。本文提出缓存驻留执行模型，将权重算子与注意力及KV缓存管理分离到专属资源域，并放松同步至子算子依赖。在多插槽CPU集群上实现原型，相比llama.cpp获得2.04x-11.51x的TPOT加速，理论模型预测最高13.9x加速。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.25353v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 755, \"height\": 1210, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25353v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 556, \"height\": 399, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25353v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 921, \"height\": 660, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25353v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 875, \"height\": 399, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25353v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1750, \"height\": 524, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25353v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 884, \"height\": 396, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25353v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 863, \"height\": 448, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25353v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 837, \"height\": 731, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25353v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 936, \"height\": 1100, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25353v1/fig-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 1680, \"height\": 388, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25353v1/fig-011.webp\", \"caption\": \"\", \"page\": 0, \"index\": 11, \"width\": 929, \"height\": 590, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25353v1/fig-012.webp\", \"caption\": \"\", \"page\": 0, \"index\": 12, \"width\": 814, \"height\": 398, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.25353v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 864, \"height\": 309, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25353v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 845, \"height\": 595, \"label\": \"Table\"}]"
motivation: 当前LLM推理受限于内存层级数据搬运，而GB级末级缓存潜力未被充分利用；深度流水线导致缓存驻留权重与KV缓存管理冲突，算子边界同步开销显著。
method: 提出缓存驻留执行模型：权重-注意力解耦架构，权重算子驻留缓存，注意力与KV独立扩展；依赖感知的同步机制，仅等待真实子算子依赖；静态运行时调度。
result: 在Llama-3.2-3B和Llama-2-7B上，TPOT加速比2.04x-11.51x；理论模型下不同规模、上下文长度和批大小加速达13.9x。
conclusion: 商品化CPU凭借GB级末级缓存，通过缓存驻留组织、解耦状态管理和依赖感知协调，即可高效支持LLM推理，无需专用加速器。
---

## 摘要
大型语言模型（LLM）推理越来越受到内存层次结构中数据移动的制约。现代服务器CPU中的3D堆叠缓存技术实现了GB级末级缓存，使得将可重用模型权重保留在芯片上并利用缓存带宽和延迟成为可能。实现这一机制并非易事：为保持权重驻留而进行更深层次的流水线化会增加正在处理的请求和KV缓存占用，而缓存驻留型算子使算子边界同步成为明显的瓶颈。我们提出了一种用于分层内存集群系统推理的缓存驻留执行模型。该模型将权重中心型算子与注意力机制和KV缓存管理分离到专用资源域中，在保持可重用权重缓存驻留的同时，独立于流水线深度扩展KV容量。它还放宽了从算子边界到真正子算子依赖关系的同步，降低了缓存驻留机制中的协调开销。我们在一个多插槽CPU集群上实例化该模型，采用了权重-注意力解耦架构、局部感知放置和专门的静态运行时。该原型显著优于同等配置的llama.cpp。在部署的Llama-3.2-3B和Llama-2-7B配置上，它在每输出token时间（TPOT）上实现了2.04倍至11.51倍的加速。在验证的分析模型下，它在不同模型大小、上下文长度和批量大小上进一步达到高达13.9倍的TPOT加速。这些结果表明，当执行围绕缓存驻留、解耦状态管理和依赖感知协调进行组织时，配备GB级末级缓存的商用CPU可以支持高效的LLM推理。

## Abstract
Large language model (LLM) inference is increasingly dominated by data movement across the memory hierarchy. Recent 3D-stacked cache technologies have enabled GB-scale last-level caches in modern server CPUs, making it possible to keep reusable model weights on chip and exploit cache bandwidth and latency. Achieving this regime is not straightforward: deeper pipelining for weight residency increases in-flight requests and KV-cache footprint, while cache-resident operators make operator-boundary synchronization a visible bottleneck.   We present a cache-resident execution model for inference on hierarchical-memory clustered systems. The model separates weight-centric operators from attention and KV-cache management into dedicated resource domains, keeping reusable weights cache-resident while scaling KV capacity independently of pipeline depth. It also relaxes synchronization from operator boundaries to true sub-operator dependencies, reducing coordination overhead in the cache-resident regime.   We instantiate this model on a multi-socket CPU cluster with a weight-attention decoupled architecture, locality-aware placement, and a specialized static runtime. The prototype substantially outperforms equally provisioned llama.cpp. On deployed Llama-3.2-3B and Llama-2-7B configurations, it achieves 2.04x-11.51x speedup on time-per-output-token (TPOT). Under a validated analytical model, it further reaches up to 13.9x TPOT speedup across model sizes, context lengths, and batch sizes. These results show that commodity CPUs with GB-scale last-level caches can support efficient LLM inference when execution is organized around cache residency, decoupled state management, and dependency-aware coordination.

---

## 论文详细总结（自动生成）

# 论文总结：Cache-Resident LLM Inference in GB-Scale Last-Level Caches

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：LLM推理受限于内存层次间的数据搬运。现代CPU通过3D堆叠缓存技术提供GB级末级缓存（LLC），理论上可以将可重用模型权重常驻芯片，利用高带宽。然而，传统实现面临两大挑战：
  1. **KV缓存压力悖论**：更深的流水线（为保持权重驻留而进行的算子分区）会增加并发请求数，导致KV缓存总占用随流水线深度线性增长，与权重竞争有限缓存资源。
  2. **算子边界同步开销**：缓存驻留后，算子执行时间急剧缩短，但算子级别的全局同步（如张量并行后的屏障）成为新的性能瓶颈，其相对开销显著增大。
- **整体含义**：本文旨在打破“LLM推理必须依赖GPU”的刻板印象，证明在商用CPU上，通过合理的资源组织、解耦状态管理和细粒度依赖感知调度，可以在GB级LLC环境中实现高效的缓存驻留推理，从而降低对稀缺加速器的依赖。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 核心思想
- **权重-注意力（Weight-Attention, WA）解耦**：将计算分离为权重中心型算子（QKV投影、FFN）和注意力中心型算子（Attention）。权重中心型算子部署在“权重节点”（weight node）上，保持模型权重缓存驻留，不存储KV缓存；注意力中心型算子部署在专用“注意力节点”（attention node）上，独立管理KV状态。这样，KV缓存容量扩展不再受流水线深度限制，权重也不会被KV缓存驱逐。
- **子算子异步性（Sub-operator Asynchronicity）**：放弃算子级别的全局同步，改为基于真实数据依赖的细粒度同步。例如，多头注意力中各头的计算独立，每个头完成后发出就绪信号，下游计算由这些信号驱动（而非全局屏障）；FFN中采用树形归约同步，将扇入度限制在小型域内（如CCD内部），以减少缓存行竞争。
- **面向缓存的运行时**：设计静态线程池，线程永久绑定物理核，利用首次接触（first touch）策略将权重分片固定在物理临近的LLC切片上；采用两级层次化同步（先CCD内，再CCD间），限制原子变量竞争范围。

### 关键技术细节
- 资源映射：集群级采用流水线并行（PP）跨机架节点；单服务器节点内利用双插槽CPU，一个插槽作权重节点，另一个作注意力节点；插槽内使用张量并行（TP）跨物理核心。
- 计算内核：GEMV中输出通道分片，激活向量常驻L1，权重从LLC流式加载；Attention采用Flash-style逐块处理KV缓存，查询向量留驻私有缓存。
- 通信：节点间通过InfiniBand RDMA传输激活张量，延迟约50μs；节点内双插槽通过共享内存和轻量同步通信。
- 模型量化：全INT8配置（含KV缓存），利用x86 VNNI指令加速。

## 3. 实验设计：数据集 / 场景、Benchmark、对比方法

- **数据集/场景**：无特定公开数据集，使用合成请求或实际模型推理，评估解码阶段（TPOT，每输出token时间）和吞吐量（tokens/s）。测试上下文长度：1024、2048、4096；批量大小：1, 2, 4, 8, 16, 32。
- **Benchmark**：与llama.cpp（广泛使用的CPU LLM推理引擎，采用算子中心执行模型）在完全相同的硬件和量化配置下比较。
- **对比方法**：
  - 端到端对比：llama.cpp vs. 本文原型（在Llama-3.2-3B和Llama-2-7B上）。
  - 消融实验：对比专用线程池 vs. OpenMP运行时；对比WA分离（双插槽） vs. 权重-KV同插槽设计。
  - 模型外推：利用验证后的分析模型，对Llama-2-70B、Qwen-3-8B等更大模型进行性能预测。
- 模型规模：Llama-3.2-3B（28层）、Llama-2-7B（32层）、Qwen-3-8B（36层）、Llama-2-70B（80层）。所有模型全INT8。

## 4. 资源与算力

- **硬件平台**：4机架服务器集群，每节点含双路AMD EPYC 9684X CPU（每个插座96核，每核12MB LLC，总LLC 1152MB）、1.5TB DDR5内存（~400GB/s带宽）、NVIDIA ConnectX-7 NIC（400Gbps InfiniBand）。未使用GPU。
- **算力说明**：文中未提及训练环节，仅涉及推理。计算资源为服务器CPU，具体利用96核AVX-512指令集。未使用任何专用加速器（如GPU、TPU）。
- **训练/预训练**：未涉及，模型为预训练后直接加载进行推理。

## 5. 实验数量与充分性

- **实验数量**：
  - 端到端测量：2个模型（3B、7B），6种批量大小，1种上下文长度（4096）——共12组主结果。
  - 消融实验：WA分离对比覆盖3个模型（3B、7B、70B），3种上下文长度，6种批量大小——共54组数据点；线程池对比覆盖4个模型（3B、7B、8B、70B），类似参数网格——约72组数据点。
  - 模型外推：通过分析模型预测4种模型、3种上下文长度、6种批量大小——72组预测值。
  - 附加分析：L2-70B单批归一化延迟分解（图11）、FLOPs/Byte强度图（图2）。
- **充分性**：
  - 正面：端到端对比真实且公平（同一硬件、同一量化）；消融实验系统化，覆盖不同模型规模、上下文和批量；分析模型经实测验证（Meas./Est.比率接近1），趋势可信。
  - 不足：端到端仅实验了较小模型（3B、7B），更大规模（70B）基于外推而非实测；仅一种对比基线（llama.cpp），未对比其他CPU推理框架（如T-Mac、FlexGen的CPU模式）；未在真实服务负载下测试（如请求到达模式、连续批处理）；仅评估解码阶段，预填充阶段提及但未深入；缺少对不同数据类型的支持（如FP16、BF16）对比。

## 6. 论文的主要结论与发现

1. **显著加速**：在Llama-3.2-3B和Llama-2-7B上，TPOT相比llama.cpp加速2.04x–11.51x，小批量下优势最大（如批量1时11.5x）。
2. **可扩展性**：基于模型外推，在不同模型、上下文和批量下最高达13.9x TPOT加速、12.5x吞吐加速；几何平均加速约3.7x–6.7x。
3. **WA分离有效**：在缓存压力较大（模型大、上下文长、批量大）时，权重-注意力解耦可减少缓存污染，带来1.13x–1.16x每块延迟改善；压力小时反而有轻微降速（因双插槽协调开销）。
4. **专用线程池有价值**：静态线程池在微秒级内核中可节省数十微秒，小批量下加速最高1.56x，大批量下效果减弱。
5. **算子边界同步是瓶颈**：缓存驻留后，算子级全局同步开销不可忽视，子算子异步性有效降低协调成本。
6. **商用CPU潜力**：商品化CPU配备GB级LLC后，通过合理组织可实现高效LLM推理，为战略稀缺的GPU分担负载。

## 7. 优点

- **系统化洞察**：清晰地识别出缓存-驻留推理的两个根本矛盾（KV压力悖论、同步开销），并针对性提出解耦和异步方案。
- **硬件中立原则**：方法论表达在内存层次和依赖关系层面，不受特定平台限制（虽实例化于CPU，但可启发其他缓存受限加速器）。
- **全面消融**：通过线程池、WA分离、分析模型等消融实验，逐一归因性能收益，验证设计选择的合理性。
- **诚实报告局限性**：承认WA分离的收益具有工作负载依赖性（缓存压力小时可能降速），且吞吐效率每插槽可能下降（由于双插槽协调开销）。
- **实际部署价值**：完全使用开源模型和商用硬件，代码基于标准工具链（RDMA、静态线程池），有直接可复现性和实用前景。

## 8. 不足与局限

- **实验覆盖有限**：
  - 端到端仅测试小模型（≤7B），70B模型性能基于外推而非真实集群部署。
  - 仅对比llama.cpp一种基线，未与更先进的CPU推理系统（如T-Mac、FlexGen的CPU模式、PagedAttention优化）比较。
  - 未在长上下文（>4K）或高并发服务场景下验证（如连续批处理、请求间KV缓存重用）。
- **功能缺失**：
  - 不支持连续批处理（在线KV缓存回收和批量重组），仅支持静态预定义批大小。
  - 预填充阶段未深度优化，仅提及“系统支持”，但重点在解码阶段。
  - 仅支持INT8量化，未探索更低位宽（如INT4、FP8）或混合精度。
- **潜在偏差**：
  - 静态运行时与OpenMP对比可能不公：OpenMP是通用方案，本文利用确定性映射绕过其调度开销，但未考虑OpenMP可调优选项（如自定义归约、锁提示）。
  - WA分离在缓存轻压下反而减速，表明该设计并非普适最优，需要运行时决策以自适应选择。
- **资源需求**：依赖GB级LLC（每个插槽1152MB），此类CPU昂贵且不普及；对于小LLC CPU或单插槽系统，收益可能大幅下降。
- **理论模型误差**：分析模型在更深的流水线（如7B）上低估TPOT约1.15x–1.52x，表明存在未建模的节点间干扰和缓存抖动。

（完）
