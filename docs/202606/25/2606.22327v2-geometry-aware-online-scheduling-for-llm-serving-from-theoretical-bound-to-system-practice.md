---
title: "Geometry-Aware Online Scheduling for LLM Serving: From Theoretical Bound to System Practice"
title_zh: 感知几何特性的LLM服务在线调度：从理论界限到系统实践
authors: "Li Kong, Qi Qi, Yinyu Ye, Zijie Zhou"
date: 2026-06-21
pdf: "https://arxiv.org/pdf/2606.22327v2"
tags: ["query:ai-infra"]
score: 10.0
evidence: KV缓存几何感知在线调度
tldr: LLM推理中KV缓存动态内存管理是性能关键，但现有时间调度启发式忽略其2D时空几何增长。本文提出几何感知的Smallest Volume First (SVF)算法及高效1-bit SVF变体，通过体积证书证明将最坏竞争比从48降至3。集成到vLLM后在Llama-3.1上实现平均和尾延迟的显著降低，为内存约束调度提供理论与实践的协同方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.22327v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1437, \"height\": 493, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22327v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1680, \"height\": 940, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22327v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1678, \"height\": 702, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.22327v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1007, \"height\": 369, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.22327v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1713, \"height\": 387, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.22327v2/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1347, \"height\": 241, \"label\": \"Table\"}]"
motivation: 现有时间调度启发式（如SJF）忽略LLM推理的2D时空几何增长，导致KV缓存内存管理低效，缺乏理论保证。
method: 提出Smallest Volume First (SVF)算法，基于体积证书证明优化竞争比，并设计仅需单比特信息的1-bit SVF变体，集成进vLLM。
result: 理论最坏竞争比从48降至3，实际在Llama-3.1模型上平均和尾延迟均显著降低，1-bit SVF接近原版性能。
conclusion: 几何感知调度理论最优且实践高效，有效解决LLM服务中内存约束调度问题，提供可复现基准。
---

## 摘要
对交互式大语言模型服务的爆炸式需求凸显了键值缓存动态内存占用管理作为推理引擎性能优化的关键领域。现代推理系统压倒性地依赖于以时间为中心的调度启发式算法，例如最短作业优先。然而，它们的理论最优性根植于传统的调度建模，未能捕捉到特定于LLM推理机制的高度动态的二维时空几何增长。为了解决这个问题，我们通过引入最小体积优先（SVF）算法及其高效变体1比特SVF，提出了感知几何特性的在线调度。在理论上，我们为我们的方法提供了严格的数学基础。通过一种新颖的体积证书证明，我们将SVF在最坏情况下的竞争比从先前最优的48大幅提升至LLM服务高并发场景下的\textbf{3}。基于这一核心突破，我们完成了一个全面的理论分类，分析了我们的算法在不同流量场景和信息可用性下的表现。在实践中，我们将我们的方法无缝集成到vLLM中，作为一个即插即用层。在Llama-3.1模型上的广泛评估展示了全面的性能提升：SVF在平均延迟和尾延迟上都实现了强有力的降低，而仅使用单个比特信息的1比特SVF实现了有竞争力的吞吐量和延迟。这项工作为解决现代LLM部署中的内存受限调度提供了一种理论上可靠且经验证有效的方法。为促进未来研究，我们的代码可在https://github.com/Aurora-Kl/Geometry-Aware-Online-Scheduling.git获取。

## Abstract
The explosive demand for interactive Large Language Model serving has highlighted the management of the Key-Value cache's dynamic memory footprint as a critical area for performance optimization in inference engines. Modern inference systems overwhelmingly rely on time-centric scheduling heuristics, such as Shortest Job First. However, their theoretical optimality is rooted in traditional schedule modeling, failing to capture the highly dynamic, 2D spatio-temporal geometric growth specific to LLM inference mechanisms. To resolve this, we propose the geometry-aware online scheduling by introducing the Smallest Volume First (SVF) algorithm and its highly efficient variant, 1-bit SVF. Theoretically, we provide a rigorous mathematical foundation for our approach. Via a novel volume-certificate proof, we sharpen SVF's worst-case competitive ratio from the prior best of 48 towards \textbf{3} in the high-concurrency regime of LLM serving. Building upon this core breakthrough, we complete a comprehensive theoretical taxonomy analyzing our algorithms across different traffic scenarios and information availability. Practically, we seamlessly integrate our approach as a plug-and-play layer in vLLM. Extensive evaluations on Llama-3.1 models demonstrate comprehensive performance gains: SVF delivers strong reductions in both average and tail latency, while 1-bit SVF, with merely a single bit information, achieves competitive throughput and latency. This work establishes a theoretically sound and empirically proven approach for resolving memory-constrained scheduling in modern LLM deployments. To facilitate future research, our code is available at https://github.com/Aurora-Kl/Geometry-Aware-Online-Scheduling.git.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：大语言模型（LLM）推理服务中，KV（Key-Value）缓存的内存占用随自回归生成动态线性增长，加之连续批处理机制导致并发大小频繁波动，形成了独特的**二维时空几何特性**。传统的时间中心型调度启发式（如最短作业优先SJF）无法捕捉这一特性，其理论最优性仅适用于传统调度模型，在LLM场景下甚至可能产生无界竞争比。
- **整体含义**：本文旨在弥合经典调度理论与LLM推理物理现实之间的鸿沟，提出一种**感知几何特性的在线调度框架**，从理论上保证最坏情况下的性能，并在实际系统中取得显著延迟和吞吐量提升。

### 2. 论文提出的方法论
- **核心思想**：将每个请求的生命周期建模为**二维体积**（Volume），即KV缓存随时间积分的总和，而不是仅考虑执行时间。优先级按体积升序排列（最小体积优先，SVF）。
- **关键技术细节**：
    - **体积公式**：给定请求 $r_i$ 的提示长度 $s_i$ 和输出长度 $o_i$，体积 $v_i = s_i \cdot o_i + \frac{o_i^2}{2} + \frac{o_i}{2}$。
    - **SVF算法**：使用预测器估计输出长度，计算体积，将请求按体积升序插入等待队列；推理引擎持续从队列头部弹出请求加入运行批次，直到KV缓存容量用完。
    - **1-bit SVF**：仅用一个二进制分类器将请求分为“短”或“长”，并分配理论推导的全局代理长度 $O_0, O_1$，计算代理体积 $\hat{v}_i = s_i \cdot O_m + \frac{O_m^2+O_m}{2}$，按此排序。
    - **理论基础**：通过**体积证书证明**（volume-certificate proof）推导出SVF的最坏竞争比为 $1 + \frac{2}{1-\alpha}$，其中 $\alpha = \max_i p_i / M$（单请求峰值内存占比）。在高并发（$\alpha \to 0$）时，竞争比趋近于**3**，大幅优于此前最优常数48。针对1-bit SVF，通过最坏竞争比 $O(T)$（$T$为最大模型长度）保证稳定性。
    - 还推导了泊松到达下的随机边界。

### 3. 实验设计
- **数据集**：
    - **LMSYS-Chat**：高并发、交互式对话流量。
    - **LongBench**：长上下文、高记忆强度的负载。
- **Benchmark**：在每个数据集上随机采样2000个请求作为测试集；剩余部分用于训练长度预测器。
- **对比方法**：
    - **基线**：FCFS（先来先服务）、SJF（最短作业优先）。
    - **Oracle**：Oracle-SJF、Oracle-SVF（使用真实输出长度，作为性能上限）。
    - **本文方法**：SVF、1-bit SVF。
- **到达模式**：
    - **突发到达**：所有请求同时到达 $t=0$。
    - **泊松到达**：请求率 $QPS \in \{2,4,8,16,32,64,128\}$（文中图2中实际使用了多个速率，具体值见图表）。
- **评估指标**：每令牌平均延迟、P95尾延迟、令牌吞吐量。

### 4. 资源与算力
- **硬件**：单台高性能节点，配备 **8块 NVIDIA A100（80GB）GPU**。
- **模型**：
    - Meta-Llama-3.1-8B-Instruct（单GPU部署，FP16/BF16）。
    - Meta-Llama-3.1-70B-Instruct（8张GPU张量并行，FP16/BF16）。
- **上下文窗口**：65536 tokens，最大生成长度4096 tokens。
- **预测器**：
    - 回归预测器：BERT-base（用于SVF）。
    - 分类器：BERT-tiny（用于1-bit SVF）。
- **训练资源**：文中未明确说明预测器训练所用的具体算力配置及时长，仅提及使用非重叠部分数据集训练。

### 5. 实验数量与充分性
- **实验数量**：覆盖了两种数据集 × 两种模型 × 两种到达模式（突发+泊松），以及多种QPS（至少6个速率点）。突发场景结果见Table 2；泊松场景结果见Figure 2（实践性能）和Figure 3（理论验证）。还有系统开销分析（Table 3）。
- **充分性**：
    - **公平性**：与FCFS、SJF等基线公平对比；Oracle版本明确上限；1-bit SVF与全预测SVF比较。
    - **全面性**：涉及小模型（8B）和大模型（70B），高并发和内存密集场景均有覆盖。
    - **客观性**：所有指标标准化为每令牌延迟，避免生成长度偏差。
    - **结论**：实验充分客观，能够支撑“SVF显著优于SJF，1-bit SVF接近SVF”的核心结论。

### 6. 论文的主要结论与发现
- **理论最优性**：SVF在最坏情况下竞争比可降至3（高并发），远优于SJF的无界比和此前最优常数48。1-bit SVF竞争比为 $O(T)$，保证稳定。
- **实践增益**：
    - SVF在所有场景下取得最低平均延迟，并在多数场景下取得最低P95尾延迟和更高吞吐量。
    - 1-bit SVF在延迟和吞吐量上均优于FCFS，在内存密集任务（LongBench）中甚至超越SJF的平均延迟。
- **公平性**：不同于传统贪心（SJF）导致长请求饿死，SVF通过快速释放KV缓存缩短全局阻塞，实际P95延迟证明系统级公平性。
- **低开销**：预测器开销极小（<=0.06%），SVF可作为即插即用模块。

### 7. 优点
- **理论突破**：首次将LLM调度竞争比从常数值48降至3，且证明方法（体积证书）干净普适。
- **方法简洁**：基于简单几何体积公式，无需复杂学习或在线优化，容易实现和集成。
- **鲁棒性**：提供突发和随机到达下的最坏与平均性能保证，算法性能不依赖特定分布。
- **轻量变体**：1-bit SVF仅需单比特分类信息，在大幅降低预测开销的同时仍保持竞争力，极具实用价值。
- **实验全面**：涵盖多种模型规模、数据集、到达模式，并排除Oracle上限干扰，验证理论正确性。

### 8. 不足与局限
- **实验覆盖**：
    - 仅在单节点（8GPU）上测试，缺少**多节点、跨GPU集群**的分布式环境验证（论文展望中承认此方向）。
    - 预测器输出长度准确性对性能有影响，但论文仅使用单一预测架构（BERT），未比较不同预测器质量下的调度鲁棒性。
- **偏差风险**：
    - 预测器训练数据来自同分布的数据集（LMSYS-Chat, LongBench），未考虑真实生产环境中的**分布偏移**。
    - 输出长度预测的误差累积可能影响SVF排序，论文未量化预测误差对竞争比的实际影响。
- **应用限制**：
    - 论文假设非抢占式调度以避免抢占开销，但某些场景（如严格SLA）可能需要抢占，本文未探讨。
    - 最大内存比例 $\alpha$ 被假设为已知且一致，实际系统中 $\alpha$ 可能因请求混合而动态变化。
- **结论**：理论与实验充分证明了SVF的有效性，但需注意以上局限，尤其部署到超大规模集群时仍需进一步验证。

（完）
