---
title: End-to-End Dynamic Sparsity for Resource-Adaptive LLM Inference
title_zh: 面向资源自适应LLM推理的端到端动态稀疏性
authors: "Yuhang Chen, Jinhao Duan, Ruichen Zhang, Mingfu Liang, Xiaohan Wei, Yunchen Pu, Fei Tian, Chonglin Sun, Parish Aggarwal, Frank Shyu, Luke Simon, Sandeep Pandey, Tianlong Chen, Xi Liu"
date: 2026-06-26
pdf: "https://arxiv.org/pdf/2606.27743v1"
tags: ["query:ai-infra"]
score: 8.0
evidence: 针对动态云基础设施的资源自适应推理
tldr: "针对LLM推理在动态云资源环境下的适应性不足，提出端到端框架L2A，将推理建模为受资源预算和输入共同约束的分配问题。通过集成轻量级、预算条件依赖且输入感知的门控网络，实现层跳过、头剪枝和推理token缩减的联合优化。单一模型在Llama-3-8B等上达到34%层稀疏度，GSM8K精度仅降0.6%且零样本泛化，而基线需单独调优且精度下降5-10%。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.27743v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 767, \"height\": 773, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27743v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1648, \"height\": 563, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27743v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1714, \"height\": 413, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27743v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1529, \"height\": 498, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.27743v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1562, \"height\": 224, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27743v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1721, \"height\": 369, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27743v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1721, \"height\": 362, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27743v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1716, \"height\": 430, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27743v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1724, \"height\": 324, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27743v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1721, \"height\": 367, \"label\": \"Table\"}]"
motivation: 静态LLM推理假设固定资源，难以应对云环境资源波动，导致崩溃或计算冗余。
method: 提出L2A框架，将推理建模为资源约束分配问题，训练预算条件化门控网络自适应调整层、头和推理token。
result: "在Llama-3-8B上34%层稀疏度时，GSM8K精度损失仅0.6%，零样本泛化至分布外任务，优于需单独调优的基线。"
conclusion: L2A实现单一模型覆盖整个计算-精度帕累托前沿，为资源自适应LLM推理提供高效方案。
---

## 摘要
大语言模型（LLM）的推理通常基于静态资源假设部署，即模型无论运行环境如何都执行固定的计算图。然而，现实世界的云基础设施本质上是动态的，其特点是可用性波动（例如，抢占式实例抢占）和分层服务质量要求。在这种不稳定的环境中，静态模型缺乏灵活性：它们要么在资源受限时崩溃，要么在冗余操作上浪费计算。为弥合这一差距，我们提出了学习分配（L2A），一种用于资源自适应推理的端到端框架。与仅基于输入难度的先前方法不同，我们将推理形式化为一个约束分配问题，其条件同时包括输入和运行时资源预算本身。我们将轻量级的、受预算条件和输入感知的门控网络集成到LLM中。这些门控通过一个统一目标进行训练，该目标沿着现实世界动态表现的三个轴同时优化任务性能、逻辑一致性和资源成本：为实现内存和深度压力而跳层，为应对吞吐量争用而剪枝头，为减少延迟而减少推理令牌。这使得模型能够学习到超越输入难度的预算感知策略：它根据实时资源动态自适应地配置其计算规模，在资源允许时最大化推理深度，在预算紧张时强制严格节约。单个L2A模型在Llama-3-8B和Qwen-3-4B上描绘了整个计算-准确率帕累托前沿：在高达34%的实际层稀疏度下，它在GSM8K上仍保持与密集基线相差0.6%的准确率，在分布外任务的零样本设置中同样保持该差距，而每种静态或启发式基线都需要单独调优的模型，并且在相当的推理时间下准确率仍下降5-10%。

## Abstract
Large Language Models (LLMs) inference is typically deployed under a static resource assumption, where models execute a fixed computational graph regardless of the runtime environment. However, real-world cloud infrastructure is inherently dynamic, characterized by fluctuating availability (e.g., spot instance preemption) and tiered Quality-of-Service requirements. In such volatile settings, static models are inflexible: they either crash under resource constraints or waste compute on redundant operations. To bridge this gap, we propose Learning to Allocate (L2A), an end-to-end framework for resource-adaptive inference. Unlike prior methods that condition only on input difficulty, we formulate inference as a constrained allocation problem conditioned on both the input and the runtime resource budget itself. We introduce lightweight, budget-conditioned and input-aware gating networks integrated into the LLM. These gates are trained via a unified objective that jointly optimizes task performance, logical consistency, and resource costs along three axes matching how real-world dynamics manifest: layer skipping for memory and depth pressure, head pruning for throughput contention, and reasoning-token reduction for latency tightening. This lets the model learn a budget-aware policy beyond input difficulty alone: it adaptively configures its computational footprint with respect to real-time resource dynamics, maximizing reasoning depth when resources permit while enforcing strict frugality when budgets tighten. A single L2A model traces the entire compute-accuracy Pareto frontier on Llama-3-8B and Qwen-3-4B: at up to 34% realized layer sparsity, it stays within 0.6% of the dense baseline on GSM8K, with the same gap holding zero-shot on out-of-distribution tasks, while every static or heuristic baseline requires a separately tuned model and still drops by 5-10% at comparable inference time.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：当前大语言模型（LLM）的推理部署通常假设计算资源是静态的，模型执行固定计算图。然而，真实的云基础设施资源高度动态（如抢占式实例中断、多租户负载波动），导致静态模型要么崩溃（OOM），要么浪费算力。现有方法（静态压缩、启发式动态推理）不能同时满足**输入难度**和**运行时资源预算**的自适应。
- **研究动机**：解决“如何让LLM在推理时根据输入难度和实时资源预算动态分配计算资源”这一关键开放问题。特别针对两个高价值场景：Spot实例抢占下的“任务生存”和不同服务质量（QoS）梯度的差异化服务。
- **整体含义**：提出L2A（Learning to Allocate）框架，将推理建模为资源约束分配问题，通过端到端训练预算条件化的门控网络，使单一模型能覆盖整个计算-精度帕累托前沿，实现优雅降级。

### 2. 论文提出的方法论

- **核心思想**：在冻结的LLM骨干上引入轻量级、**预算条件依赖**且**输入感知**的门控网络，联合优化任务损失、知识蒸馏损失和资源约束损失，以学习动态资源分配策略。
- **关键技术细节**：
    - **预算信号**：标量 `b ∈ [0,1]`（越大允许更多计算），通过嵌入 `e(b)` 输入所有门控网络。训练时从分布 `D` 采样 `b`，推理时由实时系统信号（如截止时间、队列长度、内存余量）校准映射得到。
    - **动态层跳过（Layer Skipping）**：门控 `α_l(h_{l-1}, b) = σ(MLP([h_{l-1}; e(b)]))`，训练时软门控缩放残差更新，推理时硬阈值（如0.5）二值化，跳过激活值低的层。
    - **动态头剪枝（Head Pruning）**：类似地，每个注意力头由门控 `β_{l,h}` 控制，其输出加权到MHA聚合中。
    - **动态推理到回答转换（Reasoning-to-Answer Transition）**：在生成阶段，门控 `τ_t` 决定何时强制输出 `</think><answer>` 以截断推理长度。
    - **参数高效适应**：冻结原始权重，仅训练LoRA低秩矩阵（rank=16）以适配稀疏执行路径，防止灾难性遗忘。
    - **统一优化目标**：`L(b) = L_ce + γ·L_kd + λ·|C(b)-B(b)| + λ_T·R_tok`，其中`L_ce`为交叉熵，`L_kd`为前向KL散度（教师-学生蒸馏），`C(b)`为期望计算量（层+头加权和为），`R_tok`惩罚推理段长度。
- **算法流程**：端到端训练，每个batch采样预算`b`，通过门控前向传播，计算损失更新LoRA和门控参数。推理时，根据实时预算一键切换硬门控。

### 3. 实验设计

- **数据集与场景**：
    - **分布内（ID）训练/校准**：OpenWebText（语言建模，PPL）、GSM8K（数学推理，准确率）、MMLU（知识问答）、Alpaca-Eval（指令遵循，Score）。
    - **分布外（OOD）泛化**：HumanEval（代码生成，Pass@1）、Big-Bench Hard（符号推理，准确率）。
- **基准模型**：Llama-3-8B 和 Qwen-3-4B。
- **对比方法**：
    - 原始密集模型 (Original)；
    - 静态均匀剪枝 (Uniform Pruning)；
    - Oracle静态剪枝（离线校准后的最佳固定配置）；
    - AdaSkip（基于熵的早期退出）；
    - FlexiDepth（学习型自适应层跳过）。
- **指标与设置**：质量指标（准确率/得分/PPL），效率指标（平均层/头稀疏度、峰值GPU内存、总推理时间）。在相同效率水平下公平比较，所有方法共享提示模板和生成设置。

### 4. 资源与算力

论文在实验部分**未明确说明**所使用的GPU型号、数量及训练时长。仅提到实现细节（LoRA rank=16，门控为2层MLP，温度退火调度等），但未给出算力开销。因此，**无法从文中获取具体算力信息**。

### 5. 实验数量与充分性

- **实验数量**：论文报告了多组实验，包括：
    - 主对比实验（Table 2-3）：两种模型 × 7个基线/方法，在多个指标上比较。
    - 固定预算评估（Table 4）：在0.8和0.6预算下对比固定分配与动态策略。
    - 跨域泛化实验（Table 5）：在单个源域训练后测试所有任务（Super Matrix）。
    - 消融实验（Table 6）：去除KD、预算损失、token成本、头门控的变体。
    - 训练动态可视化（Figure 3-4）：跟踪损失和平均门控值。
- **充分性判断**：  
    - **优点**：覆盖多种任务类型（语言建模、推理、代码、符号推理），评估了分布内和分布外泛化；进行了关键消融，验证了各组件必要性；与多种强基线公平比较。  
    - **不足**：缺少大规模长序列场景（如文档QA）的评估；未报告统计显著性（如多次运行的平均值和标准差）；未测量实际推理速度（wall-clock）差异说明（文中指出头剪枝加速实现依赖）。整体上实验设计客观公平，但可进一步补充不确定性分析和成本细节。

### 6. 论文的主要结论与发现

- L2A 在 Llama-3-8B 和 Qwen-3-4B 上均能在 34% 层稀疏度下保持与密集模型接近的性能（GSM8K 精度下降 <0.6%），而静态/启发式基线在类似推理时间下精度下降 5-10%。
- 单一 L2A 模型即可通过调整预算 `b` 遍历整个帕累托前沿，无需为不同预算训练多个模型。
- 学习到的门控策略能**泛化到分布外任务**（如代码生成），证明其学到了任务难度特征而非过拟合训练域。
- 消融实验表明：知识蒸馏对保真度至关重要，资源损失驱动稀疏性，联合层+头门控优于纯层跳过。

### 7. 优点

- **端到端联合优化**：将动态门控、参数高效微调和多维资源约束统一在单一损失函数中，实现协同训练。
- **预算条件化设计**：推理时可根据实时资源动态切换计算规模，无需重新训练或部署多模型。
- **跨任务泛化**：门控网络学习到输入难度特征，可迁移到未见过的任务，避免过拟合。
- **结构化稀疏控制**：同时控制层、头、推理长度三个维度，更精细地匹配不同资源瓶颈（内存、吞吐、延迟）。
- **广泛的实验验证**：覆盖 ID/OOD 任务、多种基线、消融和跨域分析，结论可靠。

### 8. 不足与局限

- **算力与效率细节缺失**：未报告训练所需 GPU 型号、数量、时长，也未报告推理时门控网络本身的计算开销或对延迟的实际影响（文中只提及层跳过和推理长度控制的加速更一致，头剪枝加速依赖实现）。
- **预算校准依赖先验估计**：假设存在合理的在线延迟/内存估计器，估计误差可能导致过激进或过保守的预算。
- **结构化格式限制**：`<think>/<answer>` 格式在某些非思考链场景通用性受限，且需一致提示，可能不适用于所有部署。
- **缺乏统计显著性**：未提供多次重复实验的标准差或置信区间，无法判断结果稳定性。
- **评估场景有限**：仅中等规模模型（4B、8B），未在更大模型（如 70B）或更长上下文场景上验证；未涉及 KV-cache 动态管理等实际部署挑战。

（完）
