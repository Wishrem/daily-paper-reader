---
title: "Geometry-Aware Online Scheduling for LLM Serving: From Theoretical Bound to System Practice"
title_zh: 面向LLM服务的几何感知在线调度：从理论界限到系统实践
authors: "Li Kong, Qi Qi, Yinyu Ye, Zijie Zhou"
date: 2026-06-21
pdf: "https://arxiv.org/pdf/2606.22327v1"
tags: ["query:ai-infra"]
score: 10.0
evidence: LLM推理中KV缓存内存管理的几何感知调度
tldr: 大语言模型推理中KV缓存的内存管理是性能优化的关键，但现有时间中心调度（如SJF）忽略了LLM推理的二维时空几何增长特性。为此提出几何感知在线调度算法SVF及其高效变体1-bit SVF。理论上采用新证明方法将已知输出长度下的最坏竞争比从48收紧至5，并完成全面理论分类。实践中作为即插即用层集成到vLLM，在Llama-3.1上显著降低平均和尾部延迟，1-bit SVF仅需单比特信息即可实现竞争性吞吐和延迟。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.22327v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1219, \"height\": 419, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22327v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1427, \"height\": 804, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22327v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1423, \"height\": 602, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.22327v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1452, \"height\": 329, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.22327v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1349, \"height\": 241, \"label\": \"Table\"}]"
motivation: 现有调度启发式（如最短作业优先）在LLM推理中忽略KV缓存动态的二维时空几何增长，导致内存受限下性能次优。
method: 提出最小体积优先（SVF）算法及其高效变体1-bit SVF，通过几何感知在线调度优化KV缓存内存占用。
result: 理论上下界竞争比从48收紧至5；在Llama-3.1上SVF显著降低平均和尾部延迟，1-bit SVF以单比特信息实现竞争性能。
conclusion: 建立了兼顾理论严谨与系统实践的LLM内存约束调度方法，代码已开源。
---

## 摘要
对交互式大型语言模型服务的爆炸性需求突显了键值缓存动态内存占用的管理作为推理引擎性能优化的关键领域。现代推理系统压倒性地依赖于以时间为中心的调度启发式方法，例如最短作业优先。然而，它们的理论最优性植根于传统的调度建模，未能捕捉到LLM推理机制特有的高度动态的二维时空几何增长。为了解决这个问题，我们提出了几何感知在线调度，引入了最小体积优先（SVF）算法及其高效变体1-bit SVF。理论上，我们为我们的方法提供了严格的数学基础。利用一种新颖的证明方法论，我们收紧了对已知输出长度的SVF的最坏情况竞争比（$\\text{CR} \\le 48 \\rightarrow \\text{CR} \\le 5$）。基于这一核心突破，我们完成了一个全面的理论分类，分析了我们的算法在不同流量场景和信息可用性下的表现。在实践中，我们将我们的方法作为即插即用层无缝集成到vLLM中。在Llama-3.1模型上的广泛评估显示了全面的性能提升：SVF显著降低了平均延迟和尾延迟，而1-bit SVF仅需单个比特信息就实现了有竞争力的吞吐量和延迟。这项工作为现代LLM部署中解决内存受限调度建立了一个理论上合理且经验验证的方法。为了促进未来的研究，我们的代码可在https://github.com/Aurora-Kl/Geometry-Aware-Online-Scheduling.git获取。

## Abstract
The explosive demand for interactive Large Language Model serving has highlighted the management of the Key-Value cache's dynamic memory footprint as a critical area for performance optimization in inference engines. Modern inference systems overwhelmingly rely on time-centric scheduling heuristics, such as Shortest Job First. However, their theoretical optimality is rooted in traditional schedule modeling, failing to capture the highly dynamic, 2D spatio-temporal geometric growth specific to LLM inference mechanisms. To resolve this, we propose the geometry-aware online scheduling by introducing the Smallest Volume First (SVF) algorithm and its highly efficient variant, 1-bit SVF. Theoretically, we provide a rigorous mathematical foundation for our approach. Utilizing a novel proof methodology, we tighten the worst-case competitive ratio ($\text{CR} \le 48 \rightarrow \text{CR} \le 5$) for SVF with known output lengths. Building upon this core breakthrough, we complete a comprehensive theoretical taxonomy analyzing our algorithms across different traffic scenarios and information availability. Practically, we seamlessly integrate our approach as a plug-and-play layer in vLLM. Extensive evaluations on Llama-3.1 models demonstrate comprehensive performance gains: SVF delivers strong reductions in both average and tail latency, while 1-bit SVF, with merely a single bit information, achieves competitive throughput and latency. This work establishes a theoretically sound and empirically proven approach for resolving memory-constrained scheduling in modern LLM deployments. To facilitate future research, our code is available at https://github.com/Aurora-Kl/Geometry-Aware-Online-Scheduling.git.

---

## 论文详细总结（自动生成）

# 论文详细中文总结：面向LLM服务的几何感知在线调度：从理论界限到系统实践

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：大语言模型（LLM）推理服务中，键值缓存（KV Cache）的动态内存占用管理是性能优化的瓶颈。现有主流调度策略（如最短作业优先 SJF）本质上是“时间中心”的，基于传统调度理论（如SRPT）的最优性，但无法适配LLM推理独特的二维时空几何特性——自回归解码使内存与时间耦合，连续批处理（continuous batching）导致批大小动态波动。
- **研究动机**：作者指出，SJF在LLM场景下的最坏竞争比（competitive ratio）被证实是**无界**的，这暴露了经典调度理论与LLM物理现实之间的根本差距。因此需要超越经验试错，建立严格的理论基础，设计“几何感知”的调度算法。
- **整体含义**：论文提出了从一维时间中心启发式到二维几何感知优先级的范式转变，并提供了严格的理论保证和系统实践验证，为内存受限的LLM部署提供了“理论完备+实验证明”的解决方案。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

### 核心思想
- 将LLM推理请求的“体积”（Volume）定义为**内存占用在时间上的积分**，即二维时空空间占用。调度优先级基于请求的体积（而非仅执行时间）进行升序排列。
- 体积公式：给定请求 `ri`，提示长度 `si`，输出长度 `oi`，则体积 `vi = si·oi + (oi² + oi)/2`。这精确捕获了预填充（prefill）和自回归解码（decode）过程中KV cache的线性增长。

### 关键技术细节
- **SVF（Smallest Volume First）算法**：
  - 严格贪心策略：将等待队列按体积升序排序，优先调度体积最小的请求。
  - 实现采用双进程设计：进程1（后台批处理预测器）批量查询输出长度预测模型（BERT-base），计算优先级并插入等待队列；进程2（LLM推理引擎）从等待队列中弹出最高优先级请求加入运行批次，直至KV cache容量满。
- **1-bit SVF（1-bit Smallest Volume First）算法**：
  - 超轻量变体：仅使用一个二进制分类器（BERT-tiny）将请求分为“短”（`m=0`）或“长”（`m=1`），然后使用全局代理长度 `O₀` 和 `O₁` 计算代理体积 `v̂_i = si·O_m + (O_m² + O_m)/2`。
  - 分类阈值 `θ = √T`（T为最大模型长度），代理长度取类内几何均值：`O₀ = T^(1/4)`，`O₁ = T^(3/4)`。
- **理论保证**：
  - 基于文献[9]的内存受限调度模型，对SVF导出新的证明方法，将最坏竞争比从48**收紧至5**（爆发到达模型，已知输出长度）。
  - 对1-bit SVF证明竞争比为 `O(T)`（T为系统最大模型长度常数），即最坏退化被系统常数永久封顶。
  - 在泊松到达（随机）模型下，对SVF和1-bit SVF分别导出期望竞争比上界，证明系统稳定性。

## 3. 实验设计：使用数据集/场景、Benchmark、对比方法

### 数据集与场景
- **数据集**：
  - **LMSYS-Chat**：高并发交互式对话流量（模拟聊天应用）。
  - **LongBench**：长文本、高内存消耗的任务（挑战服务系统）。
  - 各采样2000个请求用于服务评估，剩余部分用于训练长度预测器。
- **到达模式**：
  - **对抗性爆发（Adversarial Burst）**：所有请求在 t=0 同时到达，测试峰值调度能力与队首阻塞耐受性。
  - **随机泊松（Poisson）**：请求率 QPS ∈ {2, 8, 16, 32, 64, 128}，测试稳态性能。

### Benchmark与对比方法
- **调度基线**：
  - **FCFS**（先来先服务）
  - **SJF**（最短作业优先，经典1D启发式）
  - **Oracle-SJF** 和 **Oracle-SVF**：使用真实输出长度进行完美优先排序和预分配资源，作为理想性能上限（Oracle基线）。
- **评估指标**：
  - **每令牌延迟**（Per-token Latency）：请求端到端延迟除以实际输出令牌数，包括平均值和P95尾部值。
  - **吞吐量**（Token Throughput）：总生成令牌数除以实验时长。

### 模型
- **Meta-Llama-3.1-8B-Instruct**（单GPU部署）
- **Meta-Llama-3.1-70B-Instruct**（8 GPU张量并行部署）
- 精度FP16/BF16，上下文窗口65,536 tokens，最大生成长度4,096 tokens。

## 4. 资源与算力

论文明确说明：
- **测试环境**：单一高性能计算节点，搭载**8×NVIDIA A100 (80GB) GPU**。
- 8B模型部署在**单张A100 GPU**，70B模型使用**全部8张GPU**通过张量并行实现。
- 文中**未明确报告训练时长**（长度预测器训练时间未给出），但指出延迟预测开销极小（≤0.06%端到端时间）。

## 5. 实验数量与充分性

- **多维度实验**：
  - 两种到达模式（爆发 + 泊松） × 两个模型（8B/70B） × 两个数据集（LMSYS/LongBench） = 8个主要场景。
  - 爆发到达场景下比较了FCFS、SJF、1-bit SVF、SVF以及Oracle基线（表1）。
  - 泊松到达场景下展示了随请求率变化的延迟/吞吐量曲线（图2、图3）。
  - 进行了**消融实验**：对比SJF、SVF、Oracle-SJF、Oracle-SVF，验证理论声称（图3、表1）。
  - 计算开销剖析：在200个并发请求下测量预测器时间占端到端时间的比例（表2）。
- **充分性评价**：
  - 实验覆盖了两种典型的工作负载、两种具有挑战性的到达模式、两个规模模型。
  - 对比了多个基线，包括理论最优Oracle，区分了“调度策略本身”与“系统工程优化”的影响。
  - 支持了理论推导（Oracle-SVF比Oracle-SJF更好，验证了2D几何优于1D时间）。
  - **客观和公平**：基线选择了原始调度策略（非混合引擎优化），确保对比纯粹。Oracle基线作为上限公平参与比较。
  - 但缺乏多GPU集群扩展实验、更长上下文窗口（如128K）或更大模型（如Llama-4）的验证。

## 6. 论文的主要结论与发现

1. **SVF在所有场景下均取得最低平均延迟**，且通常最优的P95尾部延迟，吞吐量高于SJF（唯一例外是70B+LMSYS下1-bit SVF稍优尾延迟和吞吐量）。
2. **1-bit SVF** 在所有场景下优于FCFS，且在内存密集型任务（LongBench）中平均延迟优于SJF，验证了二进制分类足以有效调度。
3. **理论验证**：Oracle-SVF稳定取得最低平均延迟，而Oracle-SJF（完美知道输出长度）在LongBench下的平均延迟甚至高于**非全知的SVF**，这实证证明了“一维时间指标天然与LLM动态错配，二维几何是必要范式转换”。
4. **开销极低**：全长度预测器开销≤0.06%，1-bit分类器≤0.01%。
5. 几何感知贪心策略通过快速释放KV缓存内存，反而缓解了全局队首阻塞，实现了系统级公平性，未出现长请求无限期饥饿。
6. 理论界得到验算：SVF在爆发到达下竞争比≤5，1-bit SVF竞争比O(T)，泊松到达下SVF和1-bit SVF均保持稳定。

## 7. 优点：方法或实验设计上的亮点

- **理论贡献**：
  - 首次将LLM调度竞争比从48大幅降至5，且证明方法新颖（宏观体积速率认证，避免了传统离散化损失），为学界提供了可复用的分析框架。
  - 对1-bit SVF给出了严格的界，证明压缩为单比特不损害稳定性，具有理论深度。
- **方法设计**：
  - 体积公式精准捕获了KV cache动态增长，是物理世界语义的正确建模。
  - 双进程解耦设计避免阻塞关键推理路径，实际部署友好。
  - 1-bit SVF为资源受限场景（无算力跑大预测器）提供了极轻量替代。
- **实验设计**：
  - 设置了Oracle基线区分“调度策略”与“预测精度”的影响，隔离了贡献。
  - 消融实验直接验证理论核心论断（2D > 1D），具有说服力。
- **实用性**：代码开源，方法可即插即用到vLLM中，便于社区复现和集成。

## 8. 不足与局限

- **实验覆盖**：
  - 仅在单节点（8×A100）上测试，未扩展到多GPU集群/分布式推理场景（论文在结论中提到未来方向）。
  - 上下文窗口未使用更大值（如128K或256K），长上下文下体积计算可能改变。
  - 缺少其他模型族（如Mistral、DeepSeek）的验证，通用性待确认。
- **预测器依赖**：
  - SVF依赖输出长度预测（BERT-base），实际部署中预测偏差可能影响效果，论文未讨论预测器鲁棒性对竞争比的影响（仅在Oracle基线中完美预测）。
  - 1-bit SVF的阈值选择（√T）是在均匀分布假设下推导的，若实际输出分布极端，代理长度可能导致大量错分从而退化性能。
- **理论假设**：
  - 竞争比证明假设请求峰值内存 ≤ M/2（安全基线），在极端内存压力下不成立时，界可能变差。
  - 非抢占假设忽略了KV cache交换/重计算开销，实际系统可能有额外损失。
- **缺少与其他先进调度（如基于学习排序、预取策略）的直接对比**：论文仅对比了基本启发式和Oracle，未与近期工作如FastServe、Dynamo等系统级方案对比。
- **能耗/公平性量化**：虽提到了公平性（尾延迟改善），但未提供具体公平性指标（如Jain指数）或能效分析。

（完）
