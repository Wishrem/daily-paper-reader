---
title: "MIVE: A Minimalist Integer Vector Engine for Softmax LayerNorm and RMSNorm Acceleration"
title_zh: MIVE：一种用于Softmax、LayerNorm和RMSNorm加速的极简整数向量引擎
authors: "Kosmas Alexandridis, Giorgos Dimitrakopoulos"
date: 2026-06-16
pdf: "https://arxiv.org/pdf/2606.17781v1"
tags: ["query:ai-infra"]
score: 8.0
evidence: 针对LLM归一化操作的专用硬件加速引擎
tldr: 大型语言模型推理中，Softmax、LayerNorm和RMSNorm等非线性向量归一化操作成为硬件瓶颈。现有加速器采用独立硬件块导致资源重复和低利用率。为此，提出极简整数向量引擎MIVE，通过统一数据路径和共享计算模式，可编程执行三种操作。ASIC实现表明，MIVE在支持多函数的同时，面积效率和硬件效率均超越多数最先进的专用加速器。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.17781v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1880, \"height\": 556, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.17781v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 874, \"height\": 442, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.17781v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 744, \"height\": 379, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.17781v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 924, \"height\": 538, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.17781v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1344, \"height\": 464, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.17781v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 927, \"height\": 224, \"label\": \"Table\"}]"
motivation: 现有硬件加速器对LLM中Softmax、LayerNorm和RMSNorm使用独立模块，造成硅面积浪费和资源重复。
method: MIVE通过统一可编程数据路径，复用加、乘、查表等整数运算，实现三种归一化操作的硬件共享。
result: "ASIC实现相比独立加速器，面积降低约30%，在多种LLM上能效提升达2倍。"
conclusion: MIVE以极简硬件实现多函数加速，为LLM推理提供高效统一的向量处理方案。
---

## 摘要
大语言模型的快速增长加剧了对专用硬件加速器的需求，以应对严格的推理延迟和功耗约束。尽管矩阵乘法主导整体计算负载，但非线性向量归一化操作（如LayerNorm、RMSNorm和Softmax）可能成为关键硬件瓶颈。现有加速器通常使用专用硬件模块实现这些函数，导致资源重复和硅利用率低下。为解决这一限制，我们提出一种极简整数向量引擎（MIVE），这是一种可编程架构，能够在统一的数据通路中执行所有三种操作。通过利用LayerNorm、RMSNorm和Softmax之间的共同计算模式，所提出的向量引擎最大化硬件共享，同时减少实现开销。物理ASIC实现结果表明，MIVE提供全面的多功能支持，同时比大多数最先进的独立加速器实现更高的面积和硬件效率。

## Abstract
The rapid growth of Large Language Models (LLMs) has intensified the need for specialized hardware accelerators that can satisfy stringent inference latency and power constraints. Although matrix multiplications dominate the overall computational workload, non-linear vector normalization operations, such as LayerNorm, RMSNorm and Softmax can become critical hardware bottlenecks. Existing accelerators typically implement these functions using dedicated hardware blocks, leading to duplicated resources and inefficient silicon utilization. To address this limitation, we propose a Minimalist Integer Vector Engine (MIVE), a programmable architecture capable of executing all three operations within a unified datapath. By exploiting common computational patterns across LayerNorm, RMSNorm and Softmax the proposed vector engine maximizes hardware sharing while reducing implementation overhead. Physical ASIC implementation results show that MIVE provides comprehensive multi-function support while achieving higher area and hardware efficiency than most state-of-the-art standalone accelerators.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **问题**：大型语言模型推理中，非线性向量归一化操作（Softmax、LayerNorm、RMSNorm）常成为性能与功耗瓶颈。现有硬件加速器通常为每种操作设计独立模块，导致硅面积资源重复、利用率低下。
- **意义**：若能通过统一可编程架构共享这些操作的共性计算模式，可在支持多函数的同时降低硬件开销，提升芯片面积和能效，对LLM大规模部署至关重要。

## 2. 方法论：核心思想、关键技术细节

### 核心思想
- 将 LayerNorm、RMSNorm、Softmax 分解为两类基本原语操作：
  - **muladd**：乘加运算（可配置为加法、减法、平方、乘加等）。
  - **vecsum**：向量规约（求和、求最大值、求均值）。
- 利用这些原语在统一数据通路上共享硬件，通过指令级编程执行不同归一化流程。

### 关键技术细节
- **运算分解与映射**：
  - 三种操作均通过迭代处理子向量（长度L）实现，依赖全局统计量（均值、方差、指数和等）。
  - 设计了校正算法（LayerNorm Correction Algo.1, Softmax Correction Algo.2），用标量muladd处理校正项。
  - 使用分段线性近似（PWL）实现指数、倒数、开方倒数等非线性函数，系数存储在本地ROM。
- **硬件架构（图2）**：
  - **向量运算阵列**：L个muladd单元并行处理元素级操作；本地X寄存器存储当前子向量。
  - **标量运算单元**：单一muladd单元处理校正和归一化因子计算，连接四个局部寄存器（Mold, Mnew, Sold, Snew）。
  - **vecsum单元**：二叉树结构支持加法和减法（减法用于最大值比较），输出标量结果。
- **整数精度**：全流程INT8运算（固定点数用于中间值），输出格式与输入一致。

## 3. 实验设计

- **硬件评估**：
  - 实现：SystemVerilog RTL，28nm标准单元库，目标频率2GHz（Cadence数字实现工具）。
  - Baseline对比：Spagnolo等[13]、Kim等[14]、Softmex[4]、Shao等[9]、SOLE[8]（均为独立Softmax或归一化加速器）。
  - 指标：面积、功耗、吞吐量（GOPS）、面积效率（GOPS/mm²）、功率效率（GOPS/mW）。
- **LLM精度评估**：
  - 模型与数据集：
    - OPT-30B / LAMBADA（准确率）
    - Llama2-7B / Wikitext（困惑度）
  - 对比：FP16精确模型 vs. INT8量化 + MIVE近似。

## 4. 资源与算力

- **硬件设计资源**：使用28nm CMOS标准单元库，未提及具体GPU训练资源。仅说明ASIC物理综合和功耗估算采用Cadence工具，功耗数据来自lm-evaluation-harness框架下多个基准推理的平均值。
- **未明确**：训练LLM所需GPU型号、数量、时长；本文仅关注推理加速器的硬件实现与评估。

## 5. 实验数量与充分性

- **硬件对比实验**：
  - 一组面积/功耗/效率对比（表I），包含5个SoTA独立加速器，数据完整。
- **精度影响实验**：
  - 两组LLM任务（OPT-30B + LAMBADA，Llama2-7B + Wikitext），各只有一个量化变体对比。
  - **充分性分析**：
    - 硬件比较较充分，覆盖了近年代表性工作。
    - 精度评估略显单薄：仅两个模型、两个任务，未做不同量化策略或不同架构（如BERT）的消融；PWL近似可能带来更大误差的极端情况未讨论。

## 6. 主要结论与发现

- MIVE在28nm、2GHz下实现：
  - 面积效率332.6 GOPS/mm²，比SOLE提升2.1×，比Softmex提升12.3×。
  - 功率效率1.7 GOPS/mW，比SOLE提升2.8×。
- 同时支持Softmax、LayerNorm、RMSNorm，而大多数对比方法仅支持单一或两种操作。
- 精度损失微小：OPT-30B准确率从81%降至80%，Llama2-7B困惑度从5.8增至6.0，表明PWL近似不影响模型级性能。

## 7. 优点

- **硬件共享最大化**：通过原语分解实现统一数据通路，避免重复模块，面积效率突出。
- **可编程性**：指令级控制，易于扩展至其他类似操作（如GeLU可通过相似primitive实现）。
- **低精度友好**：全整数INT8设计，符合主流量化推理趋势。
- **对比公平**：采用与对比方法相似的工艺节点（28nm）和并行度（L=8），保持比较公平。

## 8. 不足与局限

- **实验覆盖不足**：
  - 精度评估仅两个模型、两个数据集，且只展示最终指标，缺乏对PWL分段数、位宽等参数的影响分析。
  - 未与其他联合加速器（如同时支持LayerNorm和Softmax的完整设计）在系统级性能（端到端延迟、吞吐）上比较。
- **应用限制**：
  - 仅支持INT8精度，未讨论更高精度（如INT16、FP16）场景或混合精度需求。
  - 校正算法在长向量上可能存在数值稳定性问题（如极大规模向量时均值更新误差累积），但未分析。
- **潜在偏差**：硬件效率计算使用吞吐量=硬件长度×频率，简化了真实负载下的有效吞吐（考虑读写带宽等），可能高估实际性能。

（完）
